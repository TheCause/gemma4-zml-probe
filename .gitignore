# Venvs / Python caches
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Hugging Face model weights (download separately, see README)
weights/

# Local environment / secrets
.env

# Regenerable fixtures (produced by the oracle scripts)
fixtures/*.safetensors
fixtures/*.npy
fixtures/*.npz
fixtures/*.pt

# Run logs
logs/

# Session handoffs (deposited by the session-end fallback, any directory — root included)
**/.claude-handoff.md
