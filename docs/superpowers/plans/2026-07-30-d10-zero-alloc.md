# D10 — zéro allocation par step : plan d'implémentation (rév. 2)

> Rév. 2 après revue (2 relecteurs, 17 findings, 3 BLOQUANTS) : le chemin des poids
> (`model.safetensors.index.json`) n'existait PAS sur la VM (vérifié ssh ls — toutes les
> commandes GPU auraient échoué au lancement) · Task 1.3 était inexécutable (`--out-ids` +
> `--window-vacuity` dans le même run : le mode fait `return` avant generateOnce) ET vacueuse
> (48 ids rejoués < 1024 ⇒ n_ident/q jamais émis) → le témoin V-EQ rejoue la fixture EXISTANTE
> `u9_ids.safetensors` (1150 ids, S=1178 > 1024) · Task 7.1/7.2 prescrivaient deux placements
> OPPOSÉS des init hissés vs snapshots (suivie à la lettre : AL-0 FAIL par construction) ·
> S-AC : bytes_min sans snapshot + longueur b3 périmée après resize.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal :** la boucle de décodage du 12B (et la boucle vacuity) ne fait plus AUCUN appel à
l'allocateur Zig par step, l'interdit est gardé par un compteur-gate (B9) et un critère VmRSS
(B10), et les leviers latence (allocs supprimées, mémoire pinned) sont MESURÉS.

**Architecture :** un wrapper `CountingAllocator` (module std-only) s'interpose au point unique
`init.gpa` ; le chemin B passe de `toSliceAlloc`+`@memcpy` à `toSlice` direct dans `scfg.work` ;
le top-5 passe par `getValue` (pile) ; `exe.args`/`results` sont hissés hors des boucles ; les
ArrayLists pré-réservent ; `work` est alloué via `DmaAllocator` (pinned) avec repli écrit.

**Tech stack :** Zig 0.16.0-dev.2722 (toolchain bazel rules_zig), ZML (lecture seule), RTX 3090
(`ssh ia@192.168.1.163`), bazel.

**Source de vérité des critères :** la spec
`docs/superpowers/specs/2026-07-30-d10-zero-alloc-design.md` **rév. 4** (commits `7a1437b` →
`7df0cd9`). En cas d'écart plan/spec, LA SPEC FAIT FOI (leçon : prolonger une spec revue = la
référencer). Les gates sont nommés ici par leur slug de spec (§5).

**Conventions d'exécution** (spec §8) — valables pour TOUTES les tâches :
- Build : `cd /data/rqz_workspace/zml && ./bazel.sh build -c opt --@rules_zig//zig/settings:mode=release_fast --@zml//platforms:cuda=true //examples/rqz:gemma4_g12auto //examples/rqz:gemma4_g12a4k`
- Déploiement depuis M1 : `cd ~/dev/gemma4-zml-probe/zml_runner && ./deploy_to_3090.sh`
- Chaque run de gate : capture séparée `> xxx.out.log 2> xxx.err.log`, et le log DOIT contenir
  `BUILD: mode=ReleaseFast` (sinon INEXÉCUTABLE — rebuild, ne pas requalifier).
- FAIL d'un gate ⇒ STOP, écrire le résultat, ne pas requalifier à chaud.
- Les binaires : `./bazel-bin/examples/rqz/gemma4_g12auto` (défaut 1280) et
  `./bazel-bin/examples/rqz/gemma4_g12a4k` (4k). Ils prennent DEUX arguments positionnels
  (chemins poids/tokenizer) AVANT les flags — reprendre ceux des commandes historiques :
  `/data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json`
  (vérifié : `ssh ia@192.168.1.163 'ls /data/gemma4-zml-probe/weights_12b/'` rend exactement ces
  deux fichiers ; forme d'invocation : usage du binaire `gemma4_g12auto.zig:179` et plan
  `2026-07-24-w4-j2-12b-unified.md:810`).
- Prompt des témoins EQ : `"Explique-moi la fenêtre glissante d'attention en trois phrases."`
  (`docs/FINDING_NONDETERMINISME_TRAJECTOIRE.md:170`) — auto-validé en Task 1 par cmp contre le
  témoin archivé, AVANT toute modification.

---

## Task 0 : préliminaires — provenances et DA-5

**Files :** aucun changement de code. Répertoire de travail VM : `/data/gemma4-zml-probe/d10/`
(à créer).

- [ ] **Step 0.1 : GPU libre + répertoire de travail**

```bash
ssh ia@192.168.1.163 'nvidia-smi --query-gpu=memory.used --format=csv,noheader && mkdir -p /data/gemma4-zml-probe/d10'
```
Attendu : `0 MiB` (ou < 500 MiB). Sinon : STOP, identifier le processus avant tout run.

- [ ] **Step 0.2 : DA-5 — les logs bruts S2 sont-ils récupérables ?**

```bash
ssh ia@192.168.1.163 'ls -la /tmp/s2*.out /tmp/s2*.err /data/gemma4-zml-probe/s2/ 2>/dev/null; echo "exit=$?"'
```
Si présents : `scp` vers `~/dev/gemma4-zml-probe/logs/` (noms préfixés `s2_`), commit
`docs(D10) : DA-5 — logs bruts S2 rapatriés`. Si absents : écrire le constat d'irretraçabilité
dans le doc de résultats (Task 12), ne rien inventer.

- [ ] **Step 0.3 : vérifier la présence des artefacts témoins**

```bash
ls -la logs/mi_witness_1280_ids.safetensors logs/mi_witness_4k_ids.safetensors
ssh ia@192.168.1.163 'ls -la /data/gemma4-zml-probe/rp0_witness/witness_long_before.safetensors /data/gemma4-zml-probe/rp0_witness/witness_long_before.log /data/gemma4-zml-probe/u9_ids.safetensors'
```
Attendu : les 5 fichiers existent (259 o / 564 o / 868 o / ~2,3 Ko / ~4,7 Ko —
`u9_ids.safetensors` = 1150 ids i32, la fixture du témoin V-EQ). Manquant ⇒ STOP, le signaler.

---

## Task 1 : b0 — témoins baseline (G-0, V-EQ, EQ-48 auto-validation) — AVANT toute modification

**Files :** aucun changement de code. Artefacts : dumps HLO + logs témoins.

- [ ] **Step 1.1 : build b0 (code actuel, commande §8) et vérifier le déploiement à jour**

```bash
cd ~/dev/gemma4-zml-probe/zml_runner && ./deploy_to_3090.sh
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && ./bazel.sh build -c opt --@rules_zig//zig/settings:mode=release_fast --@zml//platforms:cuda=true //examples/rqz:gemma4_g12auto //examples/rqz:gemma4_g12a4k 2>&1 | tail -3'
```
Attendu : `Build completed successfully`.
⚠ La bannière `BUILD: mode=` n'existe pas encore (elle arrive en C1) : pour b0 seulement, le mode
est attesté par la commande recopiée dans le log — l'écrire dans le doc de résultats.

- [ ] **Step 1.2 : G-0 témoin — dump HLO frais + md5 (recette S2-G réelle : `--xla_dump_to` SEUL)**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && rm -rf /tmp/d10_hlo_b0 && mkdir -p /tmp/d10_hlo_b0 && \
  XLA_FLAGS="--xla_dump_to=/tmp/d10_hlo_b0" \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Explique-moi la fenêtre glissante d'attention en trois phrases." --max-tokens 2' \
  > /tmp/d10_g0_b0.out.log 2> /tmp/d10_g0_b0.err.log
ssh ia@192.168.1.163 'ls /tmp/d10_hlo_b0 | grep -c before_optimizations && md5sum /tmp/d10_hlo_b0/module_0001.zml.before_optimizations.txt && stat -c %y /tmp/d10_hlo_b0/module_0001.zml.before_optimizations.txt'
```
Attendu : `grep -c` = **1** (un seul before_optimizations — recette et critère du gate S2-G,
plan phase2 :64-65 : md5 de `module_0001.zml.before_optimizations.txt`). Si le nom de fichier
diffère (`ls /tmp/d10_hlo_b0 | grep before` pour le voir), prendre le nom réel et le consigner.
Noter LE md5 témoin + la fraîcheur (mtime) dans le doc de résultats.

- [ ] **Step 1.3 : V-EQ témoin — rejouer la fixture 1150 ids EXISTANTE (jamais `--out-ids` ici)**

⚠ Corrigé en rév. 2 : (a) `--out-ids` + `--window-vacuity` dans le même run est IMPOSSIBLE (le
mode vacuity `return` à :1953 AVANT generateOnce, et il ouvre sa fixture dès :1836) ; (b) une
fixture de 48 ids rendrait V-EQ VACUEUX : S = 28+48 = 76 ≤ 1024 ⇒ la branche « AUCUNE
divergence » (:1950) n'émet ni `n_ident` ni `q`. Le véhicule est la fixture du témoin
historique, déjà sur la VM : `u9_ids.safetensors` (1150 ids ⇒ S = 1178 > 1024 — la zone
mordante est couverte, comme `logs/mi_witness_wv.log`).

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Explique-moi la fenêtre glissante d'attention en trois phrases." \
  --window-vacuity /data/gemma4-zml-probe/u9_ids.safetensors' \
  > /tmp/d10_veq_b0.out.log 2> /tmp/d10_veq_b0.err.log
grep -E "WINDOW-VACUITY|WV passe" /tmp/d10_veq_b0.err.log | head -20
cp /tmp/d10_veq_b0.out.log /tmp/d10_veq_b0.err.log ~/dev/gemma4-zml-probe/logs/
```
(Redirections LOCALES — le grep et le cp sont locaux eux aussi, pas de scp.) La ligne de verdict
réelle (`:1949`) : `WINDOW-VACUITY : logits bit-identiques sur <n> positions, PREMIÈRE DIVERGENCE
à q=<q> (max_abs à q : <e>)`. Attendu du témoin : **bit-identiques sur 1024 positions** et
**q=1024** (valeurs du témoin historique `mi_witness_wv.log`) ; `max_abs` noté mais HORS critère
(jitter). Ce sont ces deux champs que V-EQ (Task 11.4) comparera.

- [ ] **Step 1.4 : EQ-48 auto-validation — le b0 reproduit le témoin archivé (valide le prompt)**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Explique-moi la fenêtre glissante d'attention en trois phrases." --max-tokens 48 \
  --out-ids /data/gemma4-zml-probe/d10/eq48_b0_ids.safetensors' \
  > /tmp/d10_eq48_b0.out.log 2> /tmp/d10_eq48_b0.err.log
scp ia@192.168.1.163:/data/gemma4-zml-probe/d10/eq48_b0_ids.safetensors /tmp/
cmp /tmp/eq48_b0_ids.safetensors logs/mi_witness_1280_ids.safetensors && echo "EQ-48-B0: identique"
```
Attendu : `EQ-48-B0: identique`. Si FAIL : le prompt supposé est faux OU le témoin n'est plus
reproductible par ce binaire (jitter) — STOP, diagnostiquer (position de divergence via od) et
écrire ; ne pas continuer avec un témoin non reproduit au baseline.

- [ ] **Step 1.5 : G-0 contre-preuve — l'instrument md5 vu ÉCHOUER une fois (spec §5)**

Éditer temporairement (SANS committer) `zml_runner/gemma4_g12auto.zig:1469` — dans
`G12Step.forward`, remplacer `const h0 = e.scale(62.0).convert(.f32);` par
`const h0 = e.scale(62.0).convert(.f32).scale(1.0000001);` (op ajouté au graphe). Déployer,
builder, re-dumper (même commande que 1.2, dossier `/tmp/d10_hlo_mut`), comparer :
```bash
ssh ia@192.168.1.163 'cat /tmp/d10_hlo_mut/*before_optimizations*.txt | md5sum'
```
Attendu : md5 **différent** du témoin 1.2 → l'instrument sait échouer. Puis :
`git checkout zml_runner/gemma4_g12auto.zig` (jeter la mutation), re-déployer, re-builder,
et **prouver le retour à l'état sain** : re-dump → md5 == témoin 1.2. Consigner les deux md5.

---

## Task 2 : C1 — `alloc_count.zig` + émetteurs + sonde C8 + bannière (= b1, commit SEUL)

**Files :**
- Create : `zml_runner/alloc_count.zig`
- Modify : `zml_runner/gemma4_g12auto.zig` (lignes 25-26, 34-46, ~230 parseArgs, 1509,
  1518-1525, 1546-1553, 2026, 2069+, 2193+, 2244+, 2289+), `zml_runner/sampling.zig:53-102`
  (champ `n_shards`), `zml_runner/BUILD.bazel:527,537,547`
- Test : `--selftest-alloc-count` (S-AC, host)

- [ ] **Step 2.1 : créer `zml_runner/alloc_count.zig`**

Recopier À L'IDENTIQUE le bloc C1 de la spec §3 (le module complet, de `//! Compteur` à la
fermeture du struct). Ne rien « améliorer ».

- [ ] **Step 2.2 : l'ajouter aux 3 cibles bazel**

Dans `zml_runner/BUILD.bazel`, lignes 527, 537 et 547, ajouter `"alloc_count.zig"` à la liste
`srcs` de `gemma4_g12auto`, `gemma4_g12a4k`, `gemma4_g12a8k` (après `"sampling.zig"`).

- [ ] **Step 2.3 : installer le wrapper au point unique + bannière BUILD**

`gemma4_g12auto.zig` — en tête de fichier (près de `const sampling = @import("sampling.zig");`) :
```zig
const alloc_count = @import("alloc_count.zig");
```
Vérifier que `builtin` est déjà importé (`grep -n '@import("builtin")' zml_runner/gemma4_g12auto.zig`) ;
sinon ajouter `const builtin = @import("builtin");`.
Ligne 1509, remplacer :
```zig
    const allocator = init.gpa;
```
par :
```zig
    var counter = alloc_count.CountingAllocator.init(init.gpa);
    const allocator = counter.allocator();
    log.info("BUILD: mode={s}", .{@tagName(builtin.mode)});
```
⚠ La bannière est AVANT tous les early-returns selftest (:1527+) : chaque log de gate la porte.
⚠ ÉCART ASSUMÉ à la spec §3 Publication (une ligne `BUILD: mode=… pin=…`) : bannière SPLITTÉE en
deux lignes — `pin` n'est connu qu'après la création de la Platform (C7), or `BUILD: mode=` doit
sortir avant les selftests host qui n'ont pas de Platform. Le gate greppe `BUILD: mode=ReleaseFast`,
inchangé ; `PIN:` a sa propre ligne (conforme au C7 de la spec).

- [ ] **Step 2.4 : flag `--selftest-alloc-count` (bool) + garde --repl + usage**

Dans `parseArgs` (à côté de `--selftest-draw`, :286-289) :
```zig
        } else if (std.mem.eql(u8, a, "--selftest-alloc-count")) {
            args.selftest_alloc_count = true;
```
Champ `selftest_alloc_count: bool = false` dans le struct d'args (repérer le struct :
`grep -n "selftest_draw" zml_runner/gemma4_g12auto.zig | head -3`). Ajouter
`args.selftest_alloc_count` à la garde d'exclusivité `--repl` (:1518-1521, dans le `or`).
Ajouter une ligne à la constante `usage` (`grep -n "const usage" gemma4_g12auto.zig`) :
`--selftest-alloc-count : compteur d'allocations (host-only, S-AC)`.

- [ ] **Step 2.5 : early-return S-AC (même patron que --selftest-sampling :1543-1546)**

Après le bloc `--selftest-draw` (:1550-1553) :
```zig
    // === S-AC (D10) : le compteur compte — host-only, aucun GPU. 3 alloc + 1 resize +
    // 1 remap + 2 free via le wrapper, compteurs comparés aux attendus (spec D10 §5). ===
    if (args.selftest_alloc_count) {
        try selftestAllocCount(allocator, &counter);
        return;
    }
```
Et la fonction (près de `selftestDraw`, repérable par `grep -n "fn selftestDraw"`) :
```zig
// S-AC : exerce les 4 fonctions vtable du CountingAllocator INSTALLÉ (celui de run()) et
// vérifie les deltas. On passe par le wrapper déjà en place : c'est l'instrument de production
// qu'on teste, pas une copie.
fn selftestAllocCount(allocator: std.mem.Allocator, counter: *alloc_count.CountingAllocator) !void {
    const a0 = counter.n_alloc;
    const r0 = counter.n_resize;
    const m0 = counter.n_remap;
    const f0 = counter.n_free;
    const by0 = counter.bytes_alloc; // rév. 2 : bytes_min en DELTA, comme les autres compteurs
    const b1 = try allocator.alloc(u8, 64);
    const b2 = try allocator.alloc(u8, 128);
    var b3 = try allocator.alloc(u8, 256);
    if (allocator.resize(b3, 128)) b3 = b3[0..128]; // compte l'APPEL ; longueur mise à jour si accepté (contrat Allocator)
    if (allocator.remap(b3, 512)) |nb| b3 = nb; // idem
    allocator.free(b1);
    allocator.free(b2);
    // b3 volontairement non libéré ici — libéré après le verdict pour ne pas fausser f_delta.
    const checks = [_]struct { name: []const u8, got: u64, want: u64 }{
        .{ .name = "alloc", .got = counter.n_alloc - a0, .want = 3 },
        .{ .name = "resize", .got = counter.n_resize - r0, .want = 1 },
        .{ .name = "remap", .got = counter.n_remap - m0, .want = 1 },
        .{ .name = "free", .got = counter.n_free - f0, .want = 2 },
        .{ .name = "calls", .got = counter.calls() - (a0 + r0 + m0), .want = 5 },
        .{ .name = "bytes_min", .got = @intFromBool(counter.bytes_alloc - by0 >= 448), .want = 1 },
    };
    var pass: usize = 0;
    for (checks) |c| {
        if (c.got == c.want) {
            pass += 1;
        } else {
            log.err("S-AC: {s} = {d}, attendu {d}", .{ c.name, c.got, c.want });
        }
    }
    allocator.free(b3);
    if (pass != checks.len) {
        log.err("SELFTEST-AC: FAIL {d}/{d}", .{ pass, checks.len });
        return error.SelftestFailed;
    }
    log.info("SELFTEST-AC: PASS {d}/{d}", .{ pass, checks.len });
}
```
⚠ `remap` peut renvoyer un nouveau pointeur : réaffecter `b3` (fait ci-dessus) sinon le `free`
final serait un free d'un pointeur périmé. ⚠ Si `allocator.resize`/`remap` n'ont pas ces
signatures exactes dans cette std (`resize(mem, new_len)` / `remap(mem, new_len)`), copier la
forme depuis un appelant de la std : `grep -rn "\.resize(" <toolchain>/lib/std/array_list.zig | head -3`
— la STD FAIT FOI, consigner l'écart.

- [ ] **Step 2.6 : `n_shards` dans SamplingCfg + passage de `&counter` à generateOnce**

`sampling.zig`, dans `SamplingCfg` (après `n_cout_samples: usize = 0,`) :
```zig
    // D10 (DA-3) : nombre de shards de r_logits, capté au 1er step (r_logits est deinit
    // par step — la valeur doit être lue DANS la boucle), publié dans ALLOC-TOTAL.
    n_shards: u32 = 0,
```
`gemma4_g12auto.zig:2026` — signature de `generateOnce` : ajouter le paramètre
`counter: *alloc_count.CountingAllocator,` juste après `allocator: std.mem.Allocator,`.
Mettre à jour LES TROIS sites d'appel (`grep -n "generateOnce(" zml_runner/gemma4_g12auto.zig`
— one-shot + 2 sites REPL) en passant `&counter`.

- [ ] **Step 2.7 : deltas + émetteurs ALLOC-LOOP / ALLOC-TOTAL + capture shards**

Dans `generateOnce`, juste AVANT `while (true) : (step += 1) {` (:2077) :
```zig
    // === D10 : fenêtre ALLOC-LOOP — deltas du compteur autour de la boucle de steps SEULE
    // (les init hissés et les buffers pré-alloués sont hors fenêtre, spec §3 Publication). ===
    const al0_alloc = counter.n_alloc;
    const al0_resize = counter.n_resize;
    const al0_remap = counter.n_remap;
    const al0_free = counter.n_free;
    const al0_bytes = counter.bytes_alloc;
```
Dans la boucle, capter les shards au premier step — juste APRÈS la ligne
`var r_t5v, var r_t5i, var r_logits, ... = call_results.get(...)` (:2095-2097) :
```zig
        if (step == 0) {
            var sh_it = r_logits.shards();
            scfg.n_shards = @intCast(sh_it.remaining());
        }
```
⚠ Si `ShardIterator` n'a pas `remaining()` (`grep -n "remaining" <montage>/zml/buffer.zig`),
utiliser `@intCast(r_logits._shards.len)` (champ privé-par-convention, lisible en Zig) et le
consigner. Après la boucle, juste APRÈS la ligne `GENCFG: suppress a mordu ...` (:2289) :
```zig
    log.info("ALLOC-LOOP: alloc={d} resize={d} remap={d} free={d} bytes={d} steps={d}", .{
        counter.n_alloc - al0_alloc, counter.n_resize - al0_resize,
        counter.n_remap - al0_remap, counter.n_free - al0_free,
        counter.bytes_alloc - al0_bytes, step + 1,
    });
    log.info("ALLOC-TOTAL: alloc={d} free={d} bytes={d} shards={d}", .{
        counter.n_alloc, counter.n_free, counter.bytes_alloc, scfg.n_shards,
    });
```
⚠ ÉCART ASSUMÉ à la spec §3 (« fin de run ») : ALLOC-TOTAL est émis en fin de `generateOnce` —
équivalent en one-shot ; en `--repl`, une ligne par prompt (compteurs cumulatifs du process,
c'est la sémantique voulue pour la non-vacuité).

- [ ] **Step 2.8 : fenêtre ALLOC-VAC (boucles vacuity)**

Dans le mode `--window-vacuity`, juste AVANT `for (0..2) |pass| {` (:1881) :
```zig
        const av0_alloc = counter.n_alloc;
        const av0_resize = counter.n_resize;
        const av0_remap = counter.n_remap;
        const av0_free = counter.n_free;
        const av0_bytes = counter.bytes_alloc;
        var av_steps: usize = 0;
```
Dans la boucle interne (`for (0..s_total) |step| {`, :1889), à la fin du corps : `av_steps += 1;`.
Juste APRÈS la fermeture du `for (0..2)` :
```zig
        log.info("ALLOC-VAC: alloc={d} resize={d} remap={d} free={d} bytes={d} steps={d}", .{
            counter.n_alloc - av0_alloc, counter.n_resize - av0_resize,
            counter.n_remap - av0_remap, counter.n_free - av0_free,
            counter.bytes_alloc - av0_bytes, av_steps,
        });
```
⚠ Le mode vacuity vit dans `run()` (pas dans generateOnce) : `counter` y est directement en
scope — vérifier avec `grep -n "window_vacuity" zml_runner/gemma4_g12auto.zig | head` que le
bloc est bien dans run() ; s'il est dans une fonction séparée, lui passer `&counter` comme en 2.6.

- [ ] **Step 2.9 : sonde C8 (RSS aux tokens générés 20/200) + RSS-DELTA avant verdict A1**

Dans `generateOnce`, à côté des déclarations (:2069) :
```zig
    var rss_t20: ?u64 = null;
    var rss_t200: ?u64 = null;
```
Juste APRÈS `try generated.append(allocator, tok);` (:2244) :
```zig
        if (generated.items.len == 20) rss_t20 = mem_probe.rssKb(io);
        if (generated.items.len == 200) rss_t200 = mem_probe.rssKb(io);
```
Juste APRÈS le log `PERF : prefill ...` (:2320) et AVANT le bloc `--out-ids` (:2324) :
```zig
    // D10 (C8/AL-RSS) : émis AVANT le verdict oracle A1 — un A1Mismatch n'avale pas la mesure.
    if (rss_t20 != null and rss_t200 != null) {
        log.info("RSS-DELTA: {d} KiB (t20={d} t200={d})", .{ rss_t200.? -| rss_t20.?, rss_t20.?, rss_t200.? });
    } else {
        log.info("RSS-DELTA: INEXECUTABLE (run trop court : t20={?d} t200={?d})", .{ rss_t20, rss_t200 });
    }
```
⚠ `-|` (soustraction saturante) : si le RSS BAISSE entre t20 et t200 le delta affiche 0 — c'est
un PASS honnête (pas de dérive). `mem_probe` est déjà importé (:1818 l'utilise).

- [ ] **Step 2.10 : build + selftest local impossible (pas de zig sur M1) → build VM**

```bash
cd ~/dev/gemma4-zml-probe/zml_runner && ./deploy_to_3090.sh
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && ./bazel.sh build -c opt --@rules_zig//zig/settings:mode=release_fast --@zml//platforms:cuda=true //examples/rqz:gemma4_g12auto //examples/rqz:gemma4_g12a4k 2>&1 | tail -3'
```
Attendu : `Build completed successfully`. Toute erreur de compile : lire, corriger, re-builder —
si une signature std diffère de la spec, LA STD FAIT FOI et l'écart est consigné dans le commit.

- [ ] **Step 2.11 : commit C1 SEUL (= b1)**

```bash
cd ~/dev/gemma4-zml-probe
git add zml_runner/alloc_count.zig zml_runner/gemma4_g12auto.zig zml_runner/sampling.zig zml_runner/BUILD.bazel
git commit -m "d10(C1) : CountingAllocator + émetteurs ALLOC-* + sonde RSS + bannière BUILD — zéro correction, l'instrument d'abord"
```

---

## Task 3 : S-AC — le compteur auto-testé (host, sans GPU)

- [ ] **Step 3.1 : run S-AC**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && ./bazel-bin/examples/rqz/gemma4_g12auto /dev/null /dev/null --selftest-alloc-count' > /tmp/d10_sac.out.log 2> /tmp/d10_sac.err.log
grep -E "BUILD: mode=ReleaseFast|SELFTEST-AC: PASS 6/6" /tmp/d10_sac.err.log /tmp/d10_sac.out.log
```
Attendu : les DEUX lignes présentes. (⚠ `log.info` de Zig sort sur **stderr** : chercher dans
les deux fichiers, c'est le `.err.log` qui devrait porter les lignes.)

- [ ] **Step 3.2 : contre-preuve S-AC (mutation locale, jetée)**

Retirer `self.n_alloc += 1;` d'`alloc_count.zig`, re-déployer, re-builder, re-lancer (sortie
vers `/tmp/d10_sac_fail.{out,err}.log`) : attendu `SELFTEST-AC: FAIL` + exit ≠ 0. Puis
`git checkout zml_runner/alloc_count.zig`, re-déployer, re-builder, re-lancer : PASS. Archiver
(les logs sont LOCAUX — redirections côté M1) :
```bash
cp /tmp/d10_sac.out.log logs/d10_sac_pass.out.log
cp /tmp/d10_sac.err.log logs/d10_sac_pass.err.log
cp /tmp/d10_sac_fail.out.log logs/d10_sac_fail.out.log
cp /tmp/d10_sac_fail.err.log logs/d10_sac_fail.err.log
```

- [ ] **Step 3.3 : tag**

```bash
git tag -a gate/d10-sac-pass -m "S-AC PASS 6/6 (host) ; contre-preuve mutation n_alloc vue FAIL"
```

---

## Task 4 : fixture 200 + AL-BASE + RSS baseline (sur b1 — code encore NON corrigé)

**Files :**
- Create : `scripts/73_d10_oracle200_fixture.py`

- [ ] **Step 4.1 : mesurer positions[0] (spec C8 : la longueur se MESURE)**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Tell me the story of the number zero, from its invention to modern mathematics." --max-tokens 2' \
  > /tmp/d10_plen.out.log 2> /tmp/d10_plen.err.log
grep "PERF : prefill" /tmp/d10_plen.err.log
```
Noter `<P>` = le nombre de steps de prefill affiché (= ids.len). Le prompt vient de
`docs/superpowers/plans/2026-07-27-sampling-repetition-penalty.md:190` (provenance, spec C8).

- [ ] **Step 4.2 : écrire `scripts/73_d10_oracle200_fixture.py`**

```python
#!/usr/bin/env python3
"""D10 (C8/AL-RSS) : fabrique la fixture oracle 200 depuis le témoin witness_long_before.

positions = i32[200] avec positions[0] = <P> (longueur du prompt rendu, MESURÉE — Task 4.1) ;
le reste du tableau n'est pas consommé par le runner (seul positions[0] est lu, cf
gemma4_g12auto.zig:1699) mais le format oracle exige le tenseur complet.
fed = les 200 ids du témoin (relus depuis son safetensors, clé "ids").
Usage: python3 scripts/73_d10_oracle200_fixture.py <witness.safetensors> <P> <out.safetensors>
"""
import json
import struct
import sys


def read_ids(path):
    with open(path, "rb") as f:
        hlen = struct.unpack("<Q", f.read(8))[0]
        header = json.loads(f.read(hlen))
        info = header["ids"]
        assert info["dtype"] == "I32", info
        n = info["shape"][0]
        data = f.read()
        off = info["data_offsets"]
        return list(struct.unpack(f"<{n}i", data[off[0]:off[1]]))


def write_fixture(path, positions, fed):
    tensors = {"positions": positions, "fed": fed}
    header = {}
    blobs = []
    off = 0
    for name, vals in tensors.items():
        raw = struct.pack(f"<{len(vals)}i", *vals)
        header[name] = {"dtype": "I32", "shape": [len(vals)],
                        "data_offsets": [off, off + len(raw)]}
        blobs.append(raw)
        off += len(raw)
    hj = json.dumps(header, separators=(",", ":")).encode()
    with open(path, "wb") as f:
        f.write(struct.pack("<Q", len(hj)))
        f.write(hj)
        for b in blobs:
            f.write(b)


def main():
    witness, p_len, out = sys.argv[1], int(sys.argv[2]), sys.argv[3]
    fed = read_ids(witness)
    assert len(fed) == 200, f"témoin attendu à 200 ids, lu {len(fed)}"
    positions = [p_len] + [0] * 199
    write_fixture(out, positions, fed)
    print(f"OK: {out} (positions[0]={p_len}, fed={len(fed)} ids)")


if __name__ == "__main__":
    main()
```
Exécution :
```bash
scp ia@192.168.1.163:/data/gemma4-zml-probe/rp0_witness/witness_long_before.safetensors /tmp/
python3 scripts/73_d10_oracle200_fixture.py /tmp/witness_long_before.safetensors <P> /tmp/d10_oracle200.safetensors
scp /tmp/d10_oracle200.safetensors ia@192.168.1.163:/data/gemma4-zml-probe/d10/
```
Attendu : `OK: ... (positions[0]=<P>, fed=200 ids)`.

- [ ] **Step 4.3 : AL-BASE — le compteur voit le code actuel allouer (P1)**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Explique-moi la fenêtre glissante d'attention en trois phrases." --max-tokens 60 --top-k 1' \
  > /tmp/d10_albase.out.log 2> /tmp/d10_albase.err.log
grep -E "BUILD: mode=ReleaseFast|ALLOC-LOOP:|ALLOC-TOTAL:|S2-PONT:|GENCFG: suppress" /tmp/d10_albase.err.log
```
Verdict P1 (spec §4) : `alloc+resize+remap` ∈ **[10 ; 14] × steps** et `bytes` ∈
**[2,2 ; 2,6] MiB × steps** (steps = prefill + générés, lu dans la ligne). Hors fourchette ⇒
STOP : l'inventaire F2 était faux, retour à l'inventaire AVANT toute correction (P1 le dit).
Archiver : `scp` vers `logs/d10_albase.{out,err}.log`. Tag :
`git tag -a gate/d10-albase-pass -m "AL-BASE : <chiffres réels> — le compteur voit le code actuel allouer"`.

- [ ] **Step 4.4 : RSS-DELTA baseline (fonde le seuil P7)**

```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Tell me the story of the number zero, from its invention to modern mathematics." \
  --oracle /data/gemma4-zml-probe/d10/d10_oracle200.safetensors' \
  > /tmp/d10_rss_b1.out.log 2> /tmp/d10_rss_b1.err.log
grep -E "RSS-DELTA:|ALLOC-LOOP:" /tmp/d10_rss_b1.err.log
```
Attendu : `RSS-DELTA: <n> KiB (t20=... t200=...)` — noter `<n>` : c'est le bruit baseline qui
fonde le seuil 5 120 KiB (spec §5 AL-RSS). Si `<n>` ≥ 5 120 DÉJÀ au baseline : STOP, le seuil
est infondé — le consigner et le re-dériver AVANT de juger le binaire corrigé (ne pas juger avec
un seuil que le baseline viole). ⚠ Si le run sort en `A1Mismatch` : c'est ATTENDU comme
possibilité (bistabilité) — la ligne RSS-DELTA est émise AVANT le verdict, elle doit être là ;
un A1Mismatch n'invalide PAS cette mesure. Archiver les logs.

---

## Task 5 : C2 — chemin B : `toSlice` direct dans `scfg.work`

**Files :** Modify : `zml_runner/gemma4_g12auto.zig:2137-2146` + libellé M-COUT :2298

- [ ] **Step 5.1 : remplacer le bloc D2H du chemin B**

Remplacer (:2137-2145 — de `const t_b0` inclus à `const t_d2h` exclu, en conservant t_b0) :
```zig
            const t_b0: std.Io.Timestamp = .now(io, .awake); // M-COUT : début du bloc mesuré
            var lg_s = try r_logits.toSliceAlloc(allocator, io);
            defer lg_s.free(allocator);
            const lg = lg_s.items(f32);
            if (lg.len != scfg.work.len) {
                log.err("chemin B : logits {d} f32 != vocab {d}", .{ lg.len, scfg.work.len });
                return error.UnexpectedShape;
            }
            @memcpy(scfg.work, lg);
```
par :
```zig
            const t_b0: std.Io.Timestamp = .now(io, .awake); // M-COUT : début du bloc mesuré
            // D10 (C2) : D2H DIRECT dans work (persistant) — 0 allocation, 0 copie. toSliceAlloc
            // faisait 2 allocs d'1 MiB + 1 memcpy interne + le @memcpy ci-dessous : tout disparaît.
            // Garde en ACCEPTATION avant l'assert de Slice.init (erreur propre, pas un panic).
            const need = r_logits.shape().byteSize();
            if (need != scfg.work.len * @sizeOf(f32)) {
                log.err("chemin B : logits {d} octets != work {d} octets", .{ need, scfg.work.len * @sizeOf(f32) });
                return error.UnexpectedShape;
            }
            try r_logits.toSlice(io, zml.Slice.init(r_logits.shape(), std.mem.sliceAsBytes(scfg.work)));
```
Les usages aval (`sampling.applySuppression(scfg.work, ...)` etc.) sont INCHANGÉS.

- [ ] **Step 5.2 : libellés M-COUT (même commit — spec §2)**

Ligne :2298, remplacer `(D2H+copie {d:.1} µs, warpers {d:.1} µs)` par
`(D2H seul {d:.1} µs, warpers {d:.1} µs)`. Et le commentaire inline de fin de chrono (:2146,
`// D2H + alloc + copie`) devient `// D2H seul` — les deux dans CE commit, sinon la ligne
publiée et le code se contredisent.

- [ ] **Step 5.3 : build + commit**

Deploy + build (commande §8). Attendu : succès. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C2) : chemin B — toSlice direct dans work, 2 allocs d'1 MiB et 3 parcours supprimés ; libellé M-COUT « D2H seul »"
```

---

## Task 6 : C3 — top-5 par `getValue` (pile)

**Files :** Modify : `zml_runner/gemma4_g12auto.zig:2103-2121`

- [ ] **Step 6.1 : remplacer les deux toSliceAlloc du top-5**

Remplacer (:2103-2121 — du commentaire `// Top5 depuis le device` à la fermeture du `for` de
recopie inclus) par :
```zig
        // Top5 depuis le device (~40 octets D2H, PILE — D10/C3 : getValue délègue à toSlice,
        // zéro allocation). Gardes dtype AVANT le D2H, en acceptation ; f32 est désormais
        // vérifié aussi (il était supposé).
        if (r_t5v.shape().dtype() != .f32) {
            log.err("t5.values : dtype={s} ≠ f32 attendu", .{@tagName(r_t5v.shape().dtype())});
            return error.UnexpectedDtype;
        }
        if (r_t5i.shape().dtype() != .i32) {
            log.err("t5.indices : dtype={s} ≠ i32 attendu (topK/sort, tensor.zig:2977)", .{@tagName(r_t5i.shape().dtype())});
            return error.UnexpectedDtype;
        }
        const t5v = try r_t5v.getValue([gencfg.TOP_K]f32, io);
        const t5i = try r_t5i.getValue([gencfg.TOP_K]i32, io);
        var top5: Top5 = undefined;
        for (0..gencfg.TOP_K) |j| {
            top5.idx[j] = @intCast(t5i[j]);
            top5.val[j] = t5v[j];
        }
```
⚠ Les `defer t5v_s.free(allocator)` / `t5i_s.free(allocator)` disparaissent avec les slices.

- [ ] **Step 6.2 : build + commit**

Deploy + build. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C3) : top-5 par getValue vers la pile — 4 allocs/step supprimées, dtype f32 vérifié (il était supposé)"
```

---

## Task 7 : C4 — hisser `exe.args`/`exe.results` hors des DEUX boucles

**Files :** Modify : `zml_runner/gemma4_g12auto.zig` (:2090-2091, :2228-2229 ; :1894-1895,
:1939-1940)

- [ ] **Step 7.1 : boucle de génération**

⚠ ORDRE (rév. 2 — la rév. 1 se contredisait) : insérer les init hissés **AVANT** les snapshots
ALLOC-LOOP de 2.7, c'est-à-dire DÉPLACER les snapshots sous les init. Ordre final :
**init C4 → snapshots 2.7 → `while` (:2077)** — les init sont HORS fenêtre (spec §3), sinon
AL-0 compterait leurs 6 allocs et échouerait par construction. Le bloc à insérer :
```zig
    // D10 (C4) : args/results créés UNE FOIS — set()/call()/get() n'allouent rien (exe.zig:129-146,
    // vérifié spec F5) ; les init faisaient 6 allocs/step dont le dupe de 1006 Shapes (~293 KiB).
    var call_args = try exe.args(allocator);
    defer call_args.deinit(allocator);
    var call_results = try exe.results(allocator);
    defer call_results.deinit(allocator);
```
DANS la boucle : supprimer les lignes :2090-2091 (`var call_args = try exe.args(allocator);` et
`var call_results = try exe.results(allocator);`) et les deux deinit par step :2228-2229
(`call_args.deinit(allocator);` / `call_results.deinit(allocator);`). Le reste (`call_args.set`,
`exe.call`, `call_results.get`) est inchangé.
Auto-vérification finale de l'ordre : init hissés PUIS snapshots PUIS `while`.

- [ ] **Step 7.2 : boucle vacuity (mêmes gestes)**

Même paire d'init+defer (mêmes 4 lignes qu'en 7.1, avec le `exe` du scope vacuity), insérée
**AVANT** les snapshots ALLOC-VAC de 2.8 (ordre final : init C4 → buffer C6 (Task 9) →
snapshots 2.8 → `for (0..2)` :1881). Supprimer :1894-1895 et :1939-1940 dans la boucle interne.

- [ ] **Step 7.3 : build + commit**

Deploy + build. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C4) : exe.args/results hissés hors des 2 boucles — 6 allocs/step et ~293 KiB/step supprimés"
```

---

## Task 8 : C5 — ArrayLists : capacité réservée + `appendBounded`

**Files :** Modify : `zml_runner/gemma4_g12auto.zig` (:2051-2054, :2193, :2244)

- [ ] **Step 8.1 : réserver la capacité (borne = limit, la vraie borne des DEUX modes)**

Après les déclarations (:2051-2054) :
```zig
    // D10 (C5) : capacité réservée UNE FOIS — borne = limit (:2027), qui couvre --oracle
    // (fx.len) ET le mode libre (max_tokens). Une borne ids.len+max_tokens aurait débordé sur
    // les fixtures oracle historiques (1150 ids > max_tokens 200) — passe 1 de revue.
    // ⚠ ÉCART ASSUMÉ à la spec §3 C5 (« ids.len + limit ») : les listes ne reçoivent QUE des
    // tokens de la phase gen (:2193 gardé par in_gen_phase, :2244 après le prefill-continue) —
    // borne resserrée à limit (+1 pour gen_top5, qui reçoit s0 avant generated), déclaré ici
    // et dans le message de commit.
    try generated.ensureTotalCapacity(allocator, limit);
    try gen_top5.ensureTotalCapacity(allocator, limit + 1);
```
(`+1` sur gen_top5 : elle reçoit un élément au dernier step de prefill — s0 — AVANT que
`generated` ne reçoive le sien ; à la borne exacte, elle peut porter un élément de plus
transitoirement. Vérifier en lisant :2193 vs :2244 ; si le compte réel est ≤ limit, le `+1` est
une marge d'1 Top5 = 40 octets, sans effet.)

- [ ] **Step 8.2 : appendBounded dans la boucle**

:2193 : `try gen_top5.append(allocator, top5);` → `try gen_top5.appendBounded(top5);`
:2244 : `try generated.append(allocator, tok);` → `try generated.appendBounded(tok);`
⚠ `appendBounded` (array_list.zig:912) rend `error.OutOfMemory` si plein — garde ACTIVE en
ReleaseFast (là où `appendAssumeCapacity` serait de l'UB silencieuse, spec F4/C5).

- [ ] **Step 8.3 : build + commit**

Deploy + build. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C5) : ArrayLists pré-réservées + appendBounded — garde active en ReleaseFast ; écart assumé à la spec (borne limit, pas ids.len+limit : suffisance prouvée par :2193/:2244)"
```

---

## Task 9 : C6 — boucle vacuity : `toSlice` dans un buffer local

**Files :** Modify : `zml_runner/gemma4_g12auto.zig:1901-1907`

- [ ] **Step 9.1 : buffer local + toSlice**

AVANT `for (0..2) |pass| {` (avec les init de 7.2, AVANT les snapshots ALLOC-VAC) :
```zig
        // D10 (C6) : buffer de logits alloué UNE FOIS pour les 2 passes — hors fenêtre ALLOC-VAC.
        const wv_work = try allocator.alloc(f32, voc_us);
        defer allocator.free(wv_work);
```
Remplacer (:1901-1907) :
```zig
                var lg_s = try r_logits.toSliceAlloc(allocator, io);
                defer lg_s.free(allocator);
                const lg = lg_s.items(f32);
                if (lg.len != voc_us) {
                    log.err("WV : logits — {d} f32 != {d}", .{ lg.len, voc_us });
                    return error.UnexpectedShape;
                }
```
par :
```zig
                const need_wv = r_logits.shape().byteSize();
                if (need_wv != wv_work.len * @sizeOf(f32)) {
                    log.err("WV : logits {d} octets != {d}", .{ need_wv, wv_work.len * @sizeOf(f32) });
                    return error.UnexpectedShape;
                }
                try r_logits.toSlice(io, zml.Slice.init(r_logits.shape(), std.mem.sliceAsBytes(wv_work)));
                const lg = wv_work;
```
Les usages aval de `lg` (:1908-1927) sont inchangés.

- [ ] **Step 9.2 : build + commit**

Deploy + build. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C6) : vacuity — toSlice dans un buffer par-mode, 2 allocs d'1 MiB/step supprimées de l'instrument"
```

---

## Task 10 : C7 — pinned : `work` via DmaAllocator, repli écrit, propriété structurelle

**Files :** Modify : `zml_runner/gemma4_g12auto.zig` (:1660-1671 split ; après :1738-1748 ;
parseArgs `--no-pin`)

- [ ] **Step 10.1 : flag `--no-pin` (parseArgs + usage)**

```zig
        } else if (std.mem.eql(u8, a, "--no-pin")) {
            args.no_pin = true;
```
Champ `no_pin: bool = false` dans le struct d'args. Usage : `--no-pin : désactive l'allocation
DMA (pinned) de work — A/B de M-PIN`.

- [ ] **Step 10.2 : split du bloc SAMPLING (:1660-1671)**

Le bloc actuel garde scratch/reset/log mais PERD `scfg.work = ...` ; son defer ne garde que
scratch :
```zig
    if (scfg.pathArmed()) {
        scfg.scratch = try sampling.Scratch.init(allocator, VOCAB_CONTRACT);
        scfg.resetPerPrompt();
        log.info("SAMPLING: T={d} top_k={d} top_p={d} min_keep={d} seed={?d} (chemin complet ARMÉ ; tirage {s})", .{ scfg.temperature, scfg.top_k, scfg.top_p, scfg.min_keep, scfg.seed, if (scfg.drawArmed()) "ON" else "OFF (argmax)" });
    } else {
        log.info("SAMPLING: neutre — chemin top-5 inchangé (aucun warper, aucun tirage)", .{});
    }
    defer if (scfg.pathArmed()) scfg.scratch.deinit(allocator);
```

- [ ] **Step 10.3 : allocation pinned APRÈS la Platform (après :1748, garde CudaRequired)**

```zig
    // === D10 (C7/M-PIN) : work alloué APRÈS la Platform (DmaAllocator exige un Device vivant)
    // — le repli est ÉCRIT (ZML ne replie jamais : null → OutOfMemory, mem.zig:145-153), et
    // pin_on UNIQUE pilote l'alloc, la bannière ET le free : « libéré par l'allocateur qui a
    // alloué » est STRUCTUREL, pas puni par un panic (catch unreachable = UB en ReleaseFast). ===
    var dma = zml.mem.DmaAllocator.init(allocator, &platform.devices[0]);
    var pin_on = !args.no_pin;
    if (scfg.pathArmed()) {
        if (pin_on) {
            scfg.work = dma.allocator().alloc(f32, VOCAB_CONTRACT) catch blk: {
                pin_on = false;
                break :blk try allocator.alloc(f32, VOCAB_CONTRACT);
            };
        } else {
            scfg.work = try allocator.alloc(f32, VOCAB_CONTRACT);
        }
        if (pin_on) {
            log.info("PIN: ON (work 1 MiB dma-mappé)", .{});
        } else if (args.no_pin) {
            log.info("PIN: OFF (--no-pin)", .{});
        } else {
            log.info("PIN: OFF (alloc DMA échouée : dmaMap indisponible ou OOM transitoire)", .{});
        }
    }
    // Ordre LIFO : ce defer est déclaré APRÈS defer platform.deinit → il s'exécute AVANT lui
    // (dmaUnmap exige la plateforme vivante).
    defer if (scfg.pathArmed()) {
        if (pin_on) dma.allocator().free(scfg.work) else allocator.free(scfg.work);
    };
```
⚠ Vérifier qu'AUCUN usage de `scfg.work` n'existe entre :1671 et ce point (spec : vérifié
:1671-:1975 — re-vérifier après les édits des Tasks 5-8 : `grep -n "scfg.work" zml_runner/gemma4_g12auto.zig`).
⚠ Si `zml.mem` n'est pas le chemin d'export (`grep -n "pub const mem" <montage>/zml/zml.zig`),
utiliser le chemin réel et consigner.

- [ ] **Step 10.4 : build + commit**

Deploy + build. Puis :
```bash
git add zml_runner/gemma4_g12auto.zig
git commit -m "d10(C7) : work en mémoire DMA (pinned) avec repli écrit et propriété structurelle pin_on ; flag --no-pin (A/B M-PIN)"
```

---

## Task 11 : b2 — les gates (spec §5, critères = LA SPEC)

Tous les runs : capture séparée, `BUILD: mode=ReleaseFast` exigé dans chaque log, logs archivés
dans `logs/` (préfixe `d10_`), un tag annoté par gate PASS avec les chiffres réels.

- [ ] **Step 11.1 : build b2 + deploy** (commande §8, les 2 cibles). Attendu : succès.

- [ ] **Step 11.2 : AL-0** — même run qu'AL-BASE (Task 4.3), logs vers
  `/tmp/d10_al0_b2.{out,err}.log` (⚠ noms b2 distincts — ne pas écraser les logs b1). Grep local :
  `ALLOC-LOOP: alloc=0 resize=0 remap=0 free=0` ET `steps=` > 0 ET `ALLOC-TOTAL: alloc=` > 0.
  Tag `gate/d10-al0-pass`.

- [ ] **Step 11.3 : AL-VAC** — run vacuity : même commande que Task 1.3 (fixture
  `u9_ids.safetensors`, 2×1178 steps ⇒ `steps` > 0 garanti), logs vers
  `/tmp/d10_alvac_b2.{out,err}.log`. Grep local :
  `ALLOC-VAC: alloc=0 resize=0 remap=0 free=0` ET `steps=` > 0. Tag `gate/d10-alvac-pass`.
  ⚠ Ce run EST AUSSI le re-run de V-EQ : garder le log pour 11.4.

- [ ] **Step 11.4 : V-EQ** — comparer les champs STRUCTURELS (`n_ident`, position de première
  divergence `q`) du log 11.3 (`/tmp/d10_alvac_b2.err.log`) au témoin Task 1.3
  (`logs/d10_veq_b0.err.log`) — attendus : n_ident=1024, q=1024 des deux côtés. `max_abs` :
  publié, HORS critère (jitter). Tag `gate/d10-veq-pass`.

- [ ] **Step 11.5 : EQ-48** — même run que Task 1.4 sur b2, sortie
  `/data/gemma4-zml-probe/d10/eq48_b2_ids.safetensors`, puis :
```bash
scp ia@192.168.1.163:/data/gemma4-zml-probe/d10/eq48_b2_ids.safetensors /tmp/
cmp /tmp/eq48_b2_ids.safetensors logs/mi_witness_1280_ids.safetensors && echo "EQ-48: identique"
```
  Attendu : `EQ-48: identique`. Tag `gate/d10-eq48-pass`.

- [ ] **Step 11.6 : EQ-124** — run 4k :
```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12a4k /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Explique-moi la fenêtre glissante d'attention en trois phrases." --max-tokens 300 \
  --out-ids /data/gemma4-zml-probe/d10/eq124_b2_ids.safetensors' \
  > /tmp/d10_eq124.out.log 2> /tmp/d10_eq124.err.log
scp ia@192.168.1.163:/data/gemma4-zml-probe/d10/eq124_b2_ids.safetensors /tmp/
h1=$(od -An -tu8 -j0 -N8 /tmp/eq124_b2_ids.safetensors | tr -d ' ')
h2=$(od -An -tu8 -j0 -N8 logs/mi_witness_4k_ids.safetensors | tr -d ' ')
cmp -i $((8+h1)):$((8+h2)) -n 440 /tmp/eq124_b2_ids.safetensors logs/mi_witness_4k_ids.safetensors \
  && echo "EQ-124: identique (110 ids)"
```
  Attendu : `EQ-124: identique (110 ids)`. Si FAIL : regarder la POSITION de divergence avant
  d'accuser (au-delà de 110 = bistabilité connue, pas le chantier — spec P4). Tag
  `gate/d10-eq124-pass`.

- [ ] **Step 11.7 : EQ-PONT** — 3 runs `--top-k 1 --max-tokens 60` avec TROIS prompts
  distincts (rév. 2 — la rév. 1 en listait 2) :
  1. « Explique-moi la fenêtre glissante d'attention en trois phrases. »
  2. « Tell me the story of the number zero, from its invention to modern mathematics. »
  3. « Décris le cycle de l'eau en cinq étapes simples. » (nouveau, assumé — l'antécédent
     `suppress k ≥ 1` se vérifie au run, pas sur l'historique)
  La somme des steps_comparés doit atteindre ≥ 100. Grep par run :
  `S2-PONT: steps_comparés=<n> désaccords=0` et `GENCFG: suppress a mordu <k> fois` avec k ≥ 1
  sur AU MOINS un run (antécédent — relancer avec un prompt plus long si k=0 partout, et
  l'écrire : un antécédent au 2ᵉ essai reste un antécédent). Tag `gate/d10-eqpont-pass`.

- [ ] **Step 11.8 : G-0** — re-dump HLO sur b2 (même commande que Task 1.2, dossier
  `/tmp/d10_hlo_b2`) : md5 attendu == témoin Task 1.2. Tag `gate/d10-g0-pass`.

- [ ] **Step 11.9 : AL-RSS** — même run que Task 4.4 sur b2, logs vers
  `/tmp/d10_rss_b2.{out,err}.log`. Grep : `RSS-DELTA: <n> KiB` avec n < 5 120 (seuil confirmé
  ou re-dérivé par le baseline 4.4). Tag `gate/d10-alrss-pass`.

- [ ] **Step 11.10 : M-D10 (mesure, pas de PASS/FAIL)** — run armé complet :
```bash
ssh ia@192.168.1.163 'cd /data/rqz_workspace/zml && \
  ./bazel-bin/examples/rqz/gemma4_g12auto /data/gemma4-zml-probe/weights_12b/model.safetensors /data/gemma4-zml-probe/weights_12b/tokenizer.json \
  --prompt "Tell me the story of the number zero, from its invention to modern mathematics." \
  --max-tokens 100 --top-k 64 --top-p 0.95 --seed 42' \
  > /tmp/d10_md10.out.log 2> /tmp/d10_md10.err.log
grep -E "M-COUT:|PIN:" /tmp/d10_md10.err.log
```
  Publier `d2h_us`/`warp_us` (n ≥ 60 exigé). Verdict P3 pré-enregistré (spec §4) : [800;1450] µs
  = confirmé ; > 1450 = les allocs ne coûtaient rien de mesurable ; < 800 = elles coûtaient plus
  que le modèle. ⚠ Ce run est PIN: ON par défaut — c'est la branche A de M-PIN.

- [ ] **Step 11.11 : M-PIN (branche B)** — même run + `--no-pin`. Publier les deux `d2h_us`.
  Verdict P6 pré-enregistré : < 500 µs (PIN ON) = confirmé ; ≥ 1000 = réfuté ; [500;1000) =
  MIXTE, publié avec le chiffre. ⚠ Si la bannière du run A dit `PIN: OFF (alloc DMA échouée…)` :
  l'expérience rend son 3ᵉ verdict (indisponible) — publier tel quel, P6 sans objet, pas un échec.

---

## Task 12 : mutant M1 — trois FAIL vus (jamais committé, logs archivés)

- [ ] **Step 12.1 : appliquer le diff M1** (spec §5, recopié) : site 1 APRÈS la fermeture du
  bloc chemin B :2192 (HORS du `if (scfg.pathArmed())`), site 2 dans la boucle de steps vacuity :
```zig
        // MUTANT M1 (contre-preuve D10) — JAMAIS COMMITTÉ.
        const leak1 = try allocator.alloc(u8, 1 << 20);
        @memset(leak1, 0xAA);
```
  Deploy + build (le binaire mutant).

- [ ] **Step 12.2 : les trois FAIL** — relancer les runs AL-0, AL-VAC, AL-RSS (sorties vers
  `/tmp/d10_mut_{al0,alvac,alrss}.{out,err}.log`) : attendus `ALLOC-LOOP: alloc=<steps>` (> 0),
  `ALLOC-VAC: alloc=<steps>` (> 0), `RSS-DELTA: ~184320 KiB` (≥ 5 120). Archiver :
```bash
cp /tmp/d10_mut_al0.out.log logs/d10_mutant_al0.out.log
cp /tmp/d10_mut_al0.err.log logs/d10_mutant_al0.err.log
cp /tmp/d10_mut_alvac.out.log logs/d10_mutant_alvac.out.log
cp /tmp/d10_mut_alvac.err.log logs/d10_mutant_alvac.err.log
cp /tmp/d10_mut_alrss.out.log logs/d10_mutant_alrss.out.log
cp /tmp/d10_mut_alrss.err.log logs/d10_mutant_alrss.err.log
```

- [ ] **Step 12.3 : jeter le mutant et PROUVER le retour sain**

```bash
git checkout zml_runner/gemma4_g12auto.zig
cd zml_runner && ./deploy_to_3090.sh
```
  Re-builder, relancer AL-0 : `alloc=0` de nouveau. Commit des logs :
```bash
git add logs/d10_mutant_*
git commit -m "d10(M1) : les 3 FAIL du mutant VUS et archivés — AL-0/AL-VAC/AL-RSS savent échouer"
```

---

## Task 13 : commentaires périmés + errata (spec §2)

- [ ] **Step 13.1 :** `gemma4_g12auto.zig:25-26` (en-tête CLI) : remplacer « Seules 2 des 8
  clés » par l'état réel (6 clés sur 8, sampling + gen_config) et ajouter les flags du chantier
  (`--selftest-alloc-count`, `--no-pin`) à la liste des flags documentés.
- [ ] **Step 13.2 :** `:2094` : remplacer le commentaire « r_logits : … NON lue ici (pas de
  D2H), deinit direct » par « r_logits : lue par le chemin B (toSlice → work) quand armé ;
  deinit par step ».
- [ ] **Step 13.3 :** errata d'une ligne dans
  `docs/superpowers/plans/2026-07-29-sampling-phase2.md:544` (juste avant la table des coûts) :
  `> ⚠ ERRATA (30 juil) : les % ci-dessous reposent sur la base RÉFUTÉE (9 091 µs/step). Base corrigée et chiffres justes : spec 2026-07-29-sampling-penalty-design.md rév. 4, F16.`
- [ ] **Step 13.4 : commit**
```bash
git add zml_runner/gemma4_g12auto.zig docs/superpowers/plans/2026-07-29-sampling-phase2.md
git commit -m "d10(docs) : commentaires périmés corrigés (en-tête CLI, :2094) + errata base de coût du plan phase2"
```

---

## Task 14 : doc de résultats + mémoire + PR

- [ ] **Step 14.1 :** écrire `docs/D10_RESULTS.md` au moule de `docs/SAMPLING_RESULTS.md` :
  statut des 9 gates + 2 mesures (chiffres RÉELS), verdicts P1-P7 (confirmé/réfuté/mixte, un par
  un — une prédiction réfutée est un RÉSULTAT, écrit tel quel), dettes DA-1..DA-6 (état :
  DA-3 soldée par `shards=`, DA-5 selon Task 0.2), lien spec/plan, commande de build recopiée.
- [ ] **Step 14.2 :** mettre à jour `docs/SAMPLING_RESULTS.md` §5 : D10 → **RÉSOLUE** (renvoi
  D10_RESULTS.md) ; D3 → **SOLDÉE** (gate AL-0 porteur).
- [ ] **Step 14.3 :** `PLANNING.md` : dette D10 soldée, dettes DA-* reportées.
- [ ] **Step 14.4 : commit + push + PR**
```bash
git add docs/D10_RESULTS.md docs/SAMPLING_RESULTS.md PLANNING.md
git commit -m "docs(D10) : résultats — l'interdit « aucune allocation par step » est GARDÉ (AL-0/AL-RSS), chiffres publiés"
git push -u origin s2-d10-alloc-par-step && git push --tags
gh pr create --title "D10 : zéro allocation par step, interdit gardé (B9+B10)" --body "$(cat <<'EOF'
## Résumé
- chemin B : toSlice direct dans work (2 allocs d'1 MiB + 3 parcours supprimés)
- top-5 : getValue (pile) ; args/results hissés ; ArrayLists pré-réservées (appendBounded)
- vacuity : même motif, instrument re-validé (V-EQ)
- compteur CountingAllocator toujours actif + gates AL-0/AL-VAC/AL-RSS — l'interdit §5 est GARDÉ
- M-D10/M-PIN : verdicts P3/P6 publiés (voir docs/D10_RESULTS.md)
- spec : docs/superpowers/specs/2026-07-30-d10-zero-alloc-design.md (4 rév., 3 passes de revue, 47 findings)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```
  ⚠ Merge : décision Régis (--no-ff, comme le circuit repris à D9).

---

## Récapitulatif des gates → tags

| Gate | Tag | Task |
|---|---|---|
| S-AC | `gate/d10-sac-pass` | 3 |
| AL-BASE | `gate/d10-albase-pass` | 4 |
| AL-0 | `gate/d10-al0-pass` | 11.2 |
| AL-VAC | `gate/d10-alvac-pass` | 11.3 |
| V-EQ | `gate/d10-veq-pass` | 11.4 |
| EQ-48 | `gate/d10-eq48-pass` | 11.5 |
| EQ-124 | `gate/d10-eq124-pass` | 11.6 |
| EQ-PONT | `gate/d10-eqpont-pass` | 11.7 |
| G-0 | `gate/d10-g0-pass` | 11.8 |
| AL-RSS | `gate/d10-alrss-pass` | 11.9 |
| M-D10 / M-PIN | (mesures publiées, pas de tag de gate) | 11.10-11.11 |
