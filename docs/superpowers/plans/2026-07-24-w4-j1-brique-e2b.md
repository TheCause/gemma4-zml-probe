# W4 — Jalon J1 : brique poids 4-bit (w4a16 compressed-tensors) prouvée sur E2B — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Une brique ZML `dequantW4` (int4 groupé → bf16 en graphe) prouvée par gates W0→W4g : décode E2B-W4 complet 48/48 == HF lisant le même checkpoint, moteur `engine.zig` intact d'un octet.

**Architecture:** Pattern wrapper StepTok (précédent L3) : le checkpoint w4a16 est chargé packé (i32 + scales bf16) via `TensorStore`, la dequantization vit DANS le graphe compilé (`W4Step.forward` construit des `engine.LayerW` par valeur avec `dequantW4(pk, sc)` et appelle `forwardStep` inchangé). Trois étages d'oracles Python (bit-exact → matrice → décode HF), production du checkpoint par la recette Google exacte (recipe.yaml du 12B, rejouée sur E2B).

**Tech Stack:** ZML (workspace VM `adee932e`, patch pjrt en place — vérifié 24 juil), Zig runners Bazel, Python (venv `gemma4-probe` + venv neuf `w4quant` avec llm-compressor/compressed-tensors), Gemma-4-E2B-it-qat (source QAT bf16).

**Spec:** `docs/superpowers/specs/2026-07-18-w4-poids-4bit-12b-design.md` (ce plan = jalon J1 uniquement ; J2 = plan séparé, GO re-décidable après J1 — règle d'arrêt).

---

## 0. État du terrain (vérifié le 2026-07-24, session de réveil)

| Point | État | Preuve |
|---|---|---|
| Checkpoints HF | `google/gemma-4-12B-it-qat-w4a16-ct` + `google/gemma-4-E2B-it-qat-q4_0-unquantized` répondent 200 | curl API HF |
| df VM | `/` 18 G libres, `/data` 213 G libres — les ~10 Go du download passent sur `/data` | `df -h` |
| RAM VM | 23 Gi total, 22 Gi dispo — modèle bf16 ~10 Go passe sur CPU | `free -g` |
| Swap XLA | `/data/swapfile_xla` 24 G actif (pérennisé) | `swapon --show` |
| GPU | 0 MiB utilisés | `nvidia-smi` |
| Patch pjrt | `@setEvalBranchQuota(100_000); // local patch rqz` présent à `pjrt/pjrt.zig` (dans `structSize`) | `sed` sur la VM |
| Workspace ZML VM | `adee932` == `~/dev/zml` local — pas de bump (audit 12 juil vaut) | `git log` VM |
| venv oracles | `/data/venvs/gemma4-probe` : py3.12, transformers 5.9.0, torch 2.12.0, safetensors 0.7.0 — **sans** compressed-tensors ni llm-compressor | `pip list` |
| Tokenizer | `tokenizer.json` **identique octet à octet** entre E2B-it, E2B-qat-unquantized et 12B-w4a16-ct (sha256 `cc8d3a0c…`) | headers HF |

Accès VM : SSH direct M1→VM prouvé cette session (`ZML_JUMP` inutile). ⚠ Règle anonymisation
(repo public) : la valeur réelle `user@host` de la VM ne figure JAMAIS dans ce repo — toutes les
commandes de ce plan utilisent la variable shell `$W4R`, à définir en début de session d'exécution :

```bash
export W4R=user@gpu-host   # valeur réelle : fiche mémoire infra_rtx3090 / ~/.ssh (M1), hors repo
```

## 1. Décisions figées (issues du cadrage + cartographie 24 juil)

**D1 — Format cible = celui du 12B Google, vérifié sur pièce** (header safetensors du 12B lu par range-request) :
par Linear quantifié, 3 tenseurs : `weight_packed` I32 `[out, in/8]`, `weight_scale` BF16 `[out, in/32]`,
`weight_shape` I64 `[2]` (ignoré par ZML). Pas de `weight_zero_point` (symétrique), pas de `weight_g_idx` (RTN).
Packing little-endian : l'élément de colonne `c = 8*w + j` occupe les bits `[4j, 4j+4)` du mot `w` ;
**le nibble stocké = q + 8** (non signé ∈ [0,15]), q ∈ [-8,7]. Scale = `max_abs_du_groupe / 7.5` (ni /7 ni /8),
groupes de 32 le long de la **dim d'entrée**. Quantize = `round(clamp(x/scale, -8, 7))` avec round **half-to-even**.
Dequantize = `(nibble − 8) * scale` au dtype de la scale (bf16).

**D2 — Production du checkpoint E2B-W4 (W0)** : rejouer la recette Google exacte
(recipe.yaml du 12B : `QuantizationModifier`, group 32 symétrique, observer `memoryless_minmax`,
`ignore=[lm_head, re:.*embed.*, re:.*vision_tower.*, re:.*vision_embedder.*, re:.*audio_tower.*, re:.*router.*]`)
sur `google/gemma-4-E2B-it-qat-q4_0-unquantized` (bf16 entraîné pour le 4-bit). Voie A = `oneshot` llm-compressor
(sans dataset : weights-only statique = 0 donnée de calibration). Replis dans l'ordre : `model_free_ptq`
(shard-par-shard, sans charger le nn.Module) puis script numpy maison (même layout, validé par W0).
Conséquence de l'ignore : `embed_tokens`, `embed_tokens_per_layer` restent bf16 ; sont quantifiés
**276 linears** : les 15 couches **producers** (0-14) portent 9 Linear (`q/k/v/o_proj`,
`gate/up/down_proj`, `per_layer_input_gate`, `per_layer_projection`), les 20 couches **readers**
(15-34, YOCO `num_kv_shared_layers=20`) n'ont **PAS de modules k_proj/v_proj/k_norm** dans la
source QAT (vérifié sur le header HF : couche 20 = q/o seulement ; `modeling_gemma4.py` ne les crée
que `if not is_kv_shared_layer`) → 7 Linear chacune ; + le global `per_layer_model_projection`.
Compte : 15×9 + 20×7 + 1 = **276**. ⚠ le checkpoint E2B-it ORIGINE du repo porte, lui, des k/v
**vestigiaux** pour les 35 couches (droppés par HF au load) — les deux checkpoints n'ont pas le
même ensemble de clés, les gardes des scripts 54/55 en tiennent compte. Toutes les in_features E2B
sont divisibles par 32 (1536, 2048, 4096, 6144, 12288, 256) — contrainte `_process_group` OK.

**D3 — Chemin d'unpack ZML** : extraction **logique** (`shiftRightLogical` + AND 0xF puis `− 8`),
PAS de shift arithmétique (le nibble stocké est q+8 non signé : la propagation de signe donnerait un
décalage non uniforme). Variante `bitCast(.u4)` : mesurée à W1 à titre informatif seulement (support
backend i4/u4 non prouvé — aucun code ZML ne l'exerce).

**D4 — Flux numérique et critère W4g** : dequant produit du **bf16** (identique au poids HF-décompressé),
consommé par le chemin existant `dotPrec(fam=null)` → upcast f32 → GEMM f32. L'oracle est le **même flux**
(pattern `build_hybrid_model` : poids bf16 upcastés fp32). Précédent 1020/1020 fp32 ⇒ le critère
**token-exact 48/48** est exigible (pas d'enveloppe nécessaire à J1). Piège 17 : marge top1−top2 loggée
à chaque step ; tout flip est diagnostiqué par la marge AVANT verdict.

**D5 — « == HF » signifie HF-lisant-le-même-checkpoint-W4** (critère épistémique de la spec §3.5) :
jamais de comparaison E2B-W4 vs E2B-bf16 pour la correction. La décompression de référence est celle de
compressed-tensors (piège 12 : l'oracle importe `pack_to_int32`/`unpack`/`dequantize` de la lib, ne
re-dérive JAMAIS le format à la main).

**D6 — Deux binaires, une brique partagée** : `w4.zig` (brique + structures, fichier `srcs` partagé —
piège 18 : pas de `@import("root")`), `gemma4_w4gate` (gates W1/W2/W3, CPU), `gemma4_w4auto`
(décode complet, GPU, clone ciblé de `gemma4_gen_auto`). Noms ≤ 14c (piège 4). `engine.zig` : **0 octet**.

## 2. Structure de fichiers

```
scripts/54_w4_quantize.py        # W0 : production checkpoint E2B-W4 (recette Google) — venv w4quant
scripts/55_w4_dequant_export.py  # décompression référence → weights_w4dq (oracle) — venv w4quant
scripts/56_w4_gen_oracle.py      # fork de 49 : --weights/--device → fixture décode 48 tok — venv gemma4-probe (+ct)
scripts/57_w4_unpack_fixture.py  # fixtures W1 (étage 0) + W2 (q_proj L0) — venv w4quant
scripts/58_w4_gemm_oracle.py     # fixture W3 (GEMM q_proj L0) — venv w4quant
scripts/59_w4_corrupt_ckpt.py    # contre-test non-vacuité W4g (scale corrompu) — venv w4quant
zml_runner/w4.zig                # brique dequantW4 + W4Lin/W4LayerW/W4Model (srcs partagé)
zml_runner/gemma4_w4gate.zig     # gates W1/W2/W3 (CPU)
zml_runner/gemma4_w4auto.zig     # runner décode W4 (GPU) — clone ciblé de gemma4_gen_auto.zig
zml_runner/BUILD.bazel           # +2 cibles
docs/W4_RESULTS.md               # résultats/gates (fin de chantier)
docs/DOCUMENTATION.md            # MAJ §2/§5/§7/§8/§9/§10 (fin de chantier)
```

Chemins VM (données, hors git — rsync depuis M1, cf `G2_3_OP_SENSITIVITY.md` §8.3) :
```
/data/gemma4-zml-probe/weights_w4/model.safetensors    # checkpoint packé (sortie 54, mono-fichier)
/data/gemma4-zml-probe/weights_w4dq/model.safetensors  # décompressé bf16 clés d'origine (sortie 55)
/data/gemma4-zml-probe/fixtures/w4_*.safetensors       # fixtures W1/W2/W3
/data/gemma4-zml-probe/w4_gen48.safetensors            # fixture décode W4g (sortie 56)
/data/venvs/w4quant                                     # venv quantization (NEUF — ne pas toucher gemma4-probe)
```

## 3. Critères de PASS pré-enregistrés (avant tout run — anti-cherry-picking)

| Gate | Critère | Tag |
|---|---|---|
| W0 | Checkpoint rechargé par HF (`run_compressed=False`), décode greedy 48 tok **cohérent** (texte lisible, pas de charabia) ; header : **276**×{packed I32, scale BF16} (15 producers ×9 + 20 readers ×7 + plmp), k_proj packé présent **uniquement couches 0-14**, 0 zero_point, 0 g_idx | `gate/w4-w0-pass` |
| W1 | Unpack ZML **bit-exact** (int égalité stricte) sur fixture étage 0 ; dequant étage 0 **bit-exact bf16** (max_abs f32 == 0.0) | `gate/w4-w1-pass` |
| W2 | Dequant de **5 matrices complètes couvrant chaque famille de shape** (q_proj L0 sliding [2048,1536], q_proj L4 full [4096,1536], down_proj L20 double-wide [1536,12288], per_layer_projection L0 [1536,256], per_layer_model_projection [8960,1536]) == référence compressed-tensors, **bit-exact bf16** (CPU, déterministe). Si ULP non nul : PUBLIER, investiguer, ne pas assouplir en silence. *Déviation déclarée vs spec §3.5 étage 1 (« checkpoint entier ») : la brique est UNE fonction shape-générique — la couverture par famille de shapes + le e2e W4g remplacent le dequant des 276 matrices* | `gate/w4-w2-pass` |
| W3 | GEMM q_proj L0 (x fixture, flux bf16→f32) vs oracle torch fp32 : max_abs ≤ 1e-4, mean_abs ≤ 1e-6, 8 points fixes | `gate/w4-w3-pass` |
| W4g | Décode GPU **48/48 argmax == HF-w4dq** (oracle CPU fp32-hybride, même checkpoint) ; marge top1−top2 loggée chaque step. **Requalification pré-enregistrée** en cas de N<48 : FAIL par défaut (pas de tag) ; une requalification différentielle (précédent A2) exige TOUTES ces conditions — (a) marge top1−top2 ≤ 1e-3 à chaque step fautif, (b) comportement du re-run du même binaire documenté, (c) décision explicite de Régis consignée dans W4_RESULTS.md. Débit + VRAM `--no-prealloc` = **observations**, pas critères | `gate/w4-w4g-pass` |
| WN | `git diff --stat main -- zml_runner/engine.zig` **vide** + non-régression `gemma4_gen_auto --oracle gen_custom.safetensors` re-PASS + `smoke.sh` build OK | `gate/w4-wn-pass` |

Non-vacuité (piège 13, leçon « vacuité de l'antécédent ») : intégrée à W1 (contre-test CÂBLÉ :
`packed_corrupt` écrit par le script 57, `error.VacuousGate` si l'unpack ne diverge pas) et à W4g
(contre-test CÂBLÉ : checkpoint scale-corrompu produit par le script 59, run Step 8.2 — le décode
DOIT sortir en `error.GenMismatch`, un PASS sur checkpoint corrompu = gate vide).
Un gate qui ne peut pas échouer ne compte pas — et un contre-test doit échouer de la façon PRÉVUE
(GenMismatch), pas par crash (leçon « le contrôle qui ne peut pas réussir »).

---

### Task 0 : Terrain — venv w4quant, download source, sync scripts

**Files:** aucun fichier repo (infra VM). Branche de travail d'abord.

- [ ] **Step 0.1 : branche de chantier**

```bash
cd /Users/regis/dev/gemma4-zml-probe && git checkout -b w4-brique
```

- [ ] **Step 0.2 : venv w4quant sur la VM (séparé — ne pas polluer gemma4-probe)**

```bash
ssh $W4R 'python3 -m venv /data/venvs/w4quant && /data/venvs/w4quant/bin/pip install --upgrade pip && /data/venvs/w4quant/bin/pip install llmcompressor "compressed-tensors>=0.15.0" "transformers==5.9.0" torch accelerate safetensors sentencepiece protobuf'
```

Attendu : install OK. Noter les versions résolues (`pip list | grep -E "llmcompressor|compressed"`) dans le log de session.
Repli si conflit de deps : `pip install llmcompressor` seul (il pin ses deps), vérifier que transformers reste ≥ 5.x.

- [ ] **Step 0.3 : compressed-tensors dans le venv oracles (chargement HF seulement, léger)**

```bash
ssh $W4R '/data/venvs/gemma4-probe/bin/pip install "compressed-tensors>=0.15.0"'
```

(Requis par transformers 5.9.0 pour `CompressedTensorsConfig` — message d'erreur explicite sinon.)

- [ ] **Step 0.4 : download de la source QAT sur /data (JAMAIS sur /)**

```bash
ssh $W4R 'HF_HOME=/data/hf_cache /data/venvs/w4quant/bin/hf download google/gemma-4-E2B-it-qat-q4_0-unquantized'
```

Attendu : ~9,5 GiB dans `/data/hf_cache/hub/models--google--gemma-4-E2B-it-qat-q4_0-unquantized`.
Vérifier après coup : `ssh $W4R 'df -h / /data'` — `/` ne doit PAS avoir bougé (R8).

- [ ] **Step 0.5 : moyen de sync des scripts M1→VM** (le /data VM n'est pas un clone git)

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
```

(À relancer après chaque écriture de script — chaque task le rappelle.)

- [ ] **Step 0.6 : commit branche** (rien à committer côté code encore — sauter si vide)

---

### Task 1 : W0a — script de quantization (recette Google sur E2B)

**Files:**
- Create: `scripts/54_w4_quantize.py`

- [ ] **Step 1.1 : écrire `scripts/54_w4_quantize.py`**

```python
#!/usr/bin/env python3
"""W4 W0a — production du checkpoint E2B w4a16 compressed-tensors (jalon J1).

Recette = recipe.yaml du checkpoint Google google/gemma-4-12B-it-qat-w4a16-ct,
rejouée sur google/gemma-4-E2B-it-qat-q4_0-unquantized (bf16 entraîné pour le 4-bit) :
QuantizationModifier, int4 symétrique, strategy group, group_size 32, observer memoryless_minmax,
ignore = [lm_head, re:.*embed.*, re:.*vision_tower.*, re:.*vision_embedder.*, re:.*audio_tower.*, re:.*router.*].
Weights-only statique => AUCUNE donnée de calibration (RTN).

Venv : /data/venvs/w4quant. Lancement (VM) :
  HF_HOME=/data/hf_cache /data/venvs/w4quant/bin/python3 scripts/54_w4_quantize.py
Sortie : /data/gemma4-zml-probe/weights_w4/ (config.json + recipe.yaml + model.safetensors mono-fichier)
"""
import os
os.environ.setdefault("HF_HOME", "/data/hf_cache")

import json
import torch
from transformers import AutoTokenizer, Gemma4ForConditionalGeneration
from llmcompressor import oneshot
from llmcompressor.modifiers.quantization import QuantizationModifier
from compressed_tensors.quantization import QuantizationArgs, QuantizationScheme

SRC = "google/gemma-4-E2B-it-qat-q4_0-unquantized"
DST = "/data/gemma4-zml-probe/weights_w4"

IGNORE = [
    "lm_head",
    "re:.*embed.*",
    "re:.*vision_tower.*",
    "re:.*vision_embedder.*",
    "re:.*audio_tower.*",
    "re:.*router.*",
]

model = Gemma4ForConditionalGeneration.from_pretrained(SRC, dtype=torch.bfloat16)

recipe = QuantizationModifier(
    config_groups={
        "group_0": QuantizationScheme(
            targets=["Linear"],
            weights=QuantizationArgs(
                num_bits=4, type="int", strategy="group",
                group_size=32, symmetric=True, dynamic=False,
                observer="memoryless_minmax",
            ),
        )
    },
    ignore=IGNORE,
)
oneshot(model=model, recipe=recipe)

# max_shard_size volontairement énorme : le loader ZML (TensorRegistry.fromPath) lit UN fichier,
# comme le 12B Google (mono model.safetensors, pas d'index).
model.save_pretrained(DST, save_compressed=True, max_shard_size="20GB")
AutoTokenizer.from_pretrained(SRC).save_pretrained(DST)

# --- Auto-vérification du format produit (échoue bruyamment si le layout dévie de D1) ---
from safetensors import safe_open

path = os.path.join(DST, "model.safetensors")
assert os.path.exists(path), f"mono-fichier attendu : {path} absent (sharding non désactivé ?)"
n_packed = n_scale = n_zp = n_gidx = 0
kproj_layers = []
with safe_open(path, framework="pt") as f:
    for k in f.keys():
        if k.endswith("weight_packed"):
            n_packed += 1
            t = f.get_slice(k)
            assert t.get_dtype() == "I32", f"{k}: dtype {t.get_dtype()} != I32"
            if ".self_attn.k_proj." in k:
                kproj_layers.append(int(k.split("layers.")[1].split(".")[0]))
        elif k.endswith("weight_scale"):
            n_scale += 1
            t = f.get_slice(k)
            assert t.get_dtype() == "BF16", f"{k}: dtype {t.get_dtype()} != BF16"
        elif k.endswith("weight_zero_point"):
            n_zp += 1
        elif k.endswith("weight_g_idx"):
            n_gidx += 1
    # sonde de shape sur q_proj L0 : [2048, 1536] => packed [2048, 192], scale [2048, 48]
    qp = f.get_slice("model.language_model.layers.0.self_attn.q_proj.weight_packed")
    qs = f.get_slice("model.language_model.layers.0.self_attn.q_proj.weight_scale")
    assert list(qp.get_shape()) == [2048, 192], f"q_proj packed shape {qp.get_shape()}"
    assert list(qs.get_shape()) == [2048, 48], f"q_proj scale shape {qs.get_shape()}"

# 276 = 15 producers x 9 + 20 readers (YOCO, sans k/v) x 7 + per_layer_model_projection
assert n_packed == 276, f"{n_packed} linears packés != 276 (15x9 + 20x7 + 1)"
assert n_scale == 276 and n_zp == 0 and n_gidx == 0, f"scale={n_scale} zp={n_zp} gidx={n_gidx}"
# structure YOCO : k_proj packé sur les producers 0-14 UNIQUEMENT
assert sorted(kproj_layers) == list(range(15)), f"k_proj packés sur couches {sorted(kproj_layers)}"

qc = json.load(open(os.path.join(DST, "config.json")))["quantization_config"]
g0w = qc["config_groups"]["group_0"]["weights"]
assert qc["format"] == "pack-quantized" and g0w["group_size"] == 32 and g0w["symmetric"] is True

print(f"PASS W0a : {n_packed} linears packés (15 prod x9 + 20 readers x7 + plmp), pack-quantized g32 sym, mono-fichier OK")
```

Notes d'exécution :
- Si `observer="memoryless_minmax"` est rejeté par la version installée : retirer le paramètre
  (défaut minmax ≡ même résultat en une passe statique sur poids).
- Si `oneshot` échoue sur l'archi `Gemma4ForConditionalGeneration` (R2) : repli
  `model_free_ptq(model_stub=SRC, save_directory=DST, scheme=<même QuantizationScheme + targets regex sur les 10 familles de Linear texte>, ignore=IGNORE, device="cpu")`
  — même format on-disk. Dernier repli : script numpy maison (pack via `compressed_tensors...pack_to_int32` importé, jamais recodé).
- RAM : modèle bf16 ~10 Go sur 23 Go VM — OK, mais fermer les gros process avant.

- [ ] **Step 1.2 : sync + run sur la VM**

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
ssh $W4R 'cd /data/gemma4-zml-probe && HF_HOME=/data/hf_cache /data/venvs/w4quant/bin/python3 scripts/54_w4_quantize.py 2>&1 | tail -20'
```

Attendu : `PASS W0a : 276 linears packés…`. Le run prend plusieurs minutes (chargement + RTN + save).

- [ ] **Step 1.3 : commit**

```bash
git add scripts/54_w4_quantize.py && git commit -m "feat(w4): W0a — quantization E2B w4a16-ct (recette Google 12B rejouée, 276 linears, auto-vérifiée)"
```

---

### Task 2 : W0b — round-trip HF + oracle décode (checkpoint validé AVANT tout Zig)

**Files:**
- Create: `scripts/55_w4_dequant_export.py`
- Create: `scripts/56_w4_gen_oracle.py`

- [ ] **Step 2.1 : écrire `scripts/55_w4_dequant_export.py`**

```python
#!/usr/bin/env python3
"""W4 W0b — round-trip HF du checkpoint packé + export du checkpoint DÉCOMPRESSÉ bf16.

Charge weights_w4 via transformers + CompressedTensorsConfig(run_compressed=False) : la
décompression est celle de compressed-tensors (référence, piège 12 — jamais re-dérivée).
Exporte le state_dict dense bf16 avec les clés du checkpoint E2B d'origine
=> weights_w4dq/model.safetensors, consommable par les oracles existants (49/56) tel quel.

Venv : /data/venvs/w4quant. Lancement (VM) :
  HF_HOME=/data/hf_cache /data/venvs/w4quant/bin/python3 scripts/55_w4_dequant_export.py
"""
import os
os.environ.setdefault("HF_HOME", "/data/hf_cache")
os.environ.setdefault("HF_HUB_OFFLINE", "1")
os.environ.setdefault("TRANSFORMERS_OFFLINE", "1")

import torch
from safetensors import safe_open
from safetensors.torch import save_file
from transformers import Gemma4ForConditionalGeneration
from transformers.utils.quantization_config import CompressedTensorsConfig

SRC = "/data/gemma4-zml-probe/weights_w4"
REF = "/data/gemma4-zml-probe/weights/model.safetensors"  # E2B d'origine : liste des clés attendues
OUT_DIR = "/data/gemma4-zml-probe/weights_w4dq"
OUT = os.path.join(OUT_DIR, "model.safetensors")

model = Gemma4ForConditionalGeneration.from_pretrained(
    SRC, dtype=torch.bfloat16,
    quantization_config=CompressedTensorsConfig(run_compressed=False),
)
sd = model.state_dict()

with safe_open(REF, framework="pt") as f:
    ref_keys = set(f.keys())

# Le checkpoint E2B-it ORIGINE porte des k/v/k_norm VESTIGIAUX pour les 35 couches (droppés par
# HF au load) ; le modèle QAT kv-shared n'instancie PAS ces modules pour les readers 15-34
# (num_kv_shared_layers=20) -> ces 60 clés sont ABSENTES du state_dict, et c'est ATTENDU.
VESTIGIAL = {
    f"model.language_model.layers.{i}.self_attn.{m}.weight"
    for i in range(15, 35)
    for m in ("k_proj", "v_proj", "k_norm")
}

out, missing = {}, []
for k in sorted(ref_keys):
    if k in sd:
        t = sd[k]
    elif "lm_head" in k or k in VESTIGIAL:
        continue  # tied / vestigial YOCO — absents du modèle, c'est le comportement HF
    else:
        missing.append(k)
        continue
    assert t.dtype == torch.bfloat16, f"{k}: {t.dtype} != bf16 (décompression incomplète ?)"
    assert not torch.isnan(t.float()).any(), f"NaN dans {k}"
    out[k] = t.contiguous()

assert not missing, f"clés du checkpoint d'origine absentes du modèle décompressé : {missing[:5]}"
assert not (VESTIGIAL & set(sd)), "des clés vestigiales existent dans le state_dict — hypothèse YOCO fausse ?"
extra_packed = [k for k in sd if k.endswith(("weight_packed", "weight_scale"))]
assert not extra_packed, f"tenseurs encore packés après run_compressed=False : {extra_packed[:5]}"

os.makedirs(OUT_DIR, exist_ok=True)
save_file(out, OUT)
print(f"PASS W0b-export : {len(out)} tenseurs bf16 -> {OUT}")
```

- [ ] **Step 2.2 : écrire `scripts/56_w4_gen_oracle.py`** — copie de `scripts/49_gen_custom_oracle.py`
  avec EXACTEMENT ces changements (le reste à l'identique, convention duplication du repo) :

  1. Docstring : « W4 W0b/W4g — oracle de génération sur checkpoint arbitraire (fork du 49) ».
  2. argparse : ajouter `--weights` (défaut `/data/gemma4-zml-probe/weights/model.safetensors`)
     et `--device` choix `{cpu,auto}` défaut `cpu` (spec §3.5 : oracle CPU = déterminisme).
  3. `WEIGHTS = Path(args.weights)` (remplace la constante) ; `DEVICE = "cpu" if args.device == "cpu" else ("cuda" if torch.cuda.is_available() else "cpu")`.
  4. `MODEL_ID` inchangé (`google/gemma-4-E2B-it`) : config archi et tokenizer identiques à la
     source QAT (vérifié sur pièce — sha256 tokenizer identique, config identique hors renommage
     de clé nulle). Le manifest de sortie doit consigner `weights` et `device`.

- [ ] **Step 2.3 : sync + run W0b**

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
ssh $W4R 'cd /data/gemma4-zml-probe && HF_HOME=/data/hf_cache /data/venvs/w4quant/bin/python3 scripts/55_w4_dequant_export.py 2>&1 | tail -5'
ssh $W4R 'cd /data/gemma4-zml-probe && source /data/venvs/gemma4-probe/bin/activate && HF_HOME=/data/hf_cache python3 scripts/56_w4_gen_oracle.py --weights /data/gemma4-zml-probe/weights_w4dq/model.safetensors --prompt "What is the capital of France? Answer in one word." --n-tokens 48 --out /data/gemma4-zml-probe/w4_gen48.safetensors 2>&1 | tail -15'
ssh $W4R 'cd /data/gemma4-zml-probe && source /data/venvs/gemma4-probe/bin/activate && python3 scripts/48_detokenize.py /data/gemma4-zml-probe/w4_gen48.safetensors 2>&1 | tail -5'
```

Attendu : export PASS ; génération 48 tokens ; détok = réponse **cohérente** (le modèle QAT doit dire
« Paris » ou proche — un charabia = W0 FAIL ⇒ investiguer la recette, PAS continuer).
Garder la sortie détokenisée dans le log de session (preuve W0).

- [ ] **Step 2.4 : commit + tag**

```bash
git add scripts/55_w4_dequant_export.py scripts/56_w4_gen_oracle.py
git commit -m "feat(w4): W0b — round-trip HF (run_compressed=False) + export dequant + oracle décode 48 tok : checkpoint E2B-W4 VALIDE"
git tag gate/w4-w0-pass
```

---

### Task 3 : fixtures W1/W2 (oracle étage 0 + matrice réelle)

**Files:**
- Create: `scripts/57_w4_unpack_fixture.py`

- [ ] **Step 3.1 : écrire `scripts/57_w4_unpack_fixture.py`**

```python
#!/usr/bin/env python3
"""W4 W1/W2 — fixtures d'unpack/dequant.

Étage 0 (W1) : 2 rangées x 32 colonnes de q int4 CHOISIS (couvre les 16 nibbles, le bit 31 du mot
i32, les deux extrêmes -8/7), packés par la RÉFÉRENCE compressed-tensors (pack_to_int32 importé,
jamais recodé — piège 12). Expected = q int8 + dequant bf16.
Étage 1 (W2) : expected dequant bf16 de 5 matrices COMPLÈTES du checkpoint weights_w4, une par
famille de shape (déviation déclarée vs spec §3.5 « entier » — plan §3), décompressées par la
référence (le runner lira packed/scale directement dans le checkpoint).

Contre-test de non-vacuité W1 : une copie CORROMPUE du packed (1 nibble flippé) est aussi écrite ;
le runner doit diverger dessus, sinon le gate est vide.

Venv : /data/venvs/w4quant. Lancement (VM) :
  /data/venvs/w4quant/bin/python3 scripts/57_w4_unpack_fixture.py
Sorties : fixtures/w4_unpack.safetensors (+ manifest), fixtures/w4_qproj0.safetensors (+ manifest)
"""
import json
import os

import torch
from safetensors import safe_open
from safetensors.torch import save_file

from compressed_tensors.compressors.pack_quantized import pack_to_int32, unpack_from_int32

ROOT = "/data/gemma4-zml-probe"
CKPT = os.path.join(ROOT, "weights_w4", "model.safetensors")
FX = os.path.join(ROOT, "fixtures")
os.makedirs(FX, exist_ok=True)

# ---------- Étage 0 : groupes choisis ----------
row0 = torch.tensor(list(range(-8, 8)) + list(range(7, -9, -1)), dtype=torch.int8)  # 32 valeurs
torch.manual_seed(1337)
row1 = torch.randint(-8, 8, (32,), dtype=torch.int8)
row1[7] = 7   # nibble 7 du mot 0 = 15 -> bit 31 du i32 à 1 (piège du signe)
row1[15] = 7  # idem mot 1
q = torch.stack([row0, row1])                                   # [2, 32] int8
packed = pack_to_int32(q.clone(), num_bits=4)                    # [2, 4] int32 (réf : q+8, LE nibble)
scales = torch.tensor([[0.5], [0.033203125]], dtype=torch.bfloat16)  # [2, 1] (1 groupe de 32/rangée)
deq = q.to(torch.bfloat16) * scales                              # dequant réf : (nibble-8)*scale, bf16

corrupt = packed.clone()
corrupt[0, 0] ^= 0x000000F0  # flippe le nibble 1 (colonne 1) de la rangée 0

save_file(
    {
        "packed": packed.contiguous(), "scales": scales.contiguous(),
        "q_expected": q.contiguous(), "deq_expected": deq.contiguous(),
        "packed_corrupt": corrupt.contiguous(),
    },
    os.path.join(FX, "w4_unpack.safetensors"),
)
json.dump(
    {
        "source": "57_w4_unpack_fixture.py étage 0",
        "layout": "compressed-tensors pack-quantized : nibble j = bits [4j,4j+4), stocke q+8",
        "pass": "unpack ZML == q_expected (int, bit-exact) ; dequant ZML == deq_expected (bf16, bit-exact) ; "
                "packed_corrupt DOIT diverger (non-vacuité)",
        "tensors": {"packed": [2, 4], "scales": [2, 1], "q_expected": [2, 32], "deq_expected": [2, 32]},
    },
    open(os.path.join(FX, "w4_unpack_manifest.json"), "w"), indent=2,
)

# selfcheck étage 0 : la référence doit se relire elle-même
q_rt = unpack_from_int32(packed, num_bits=4, shape=torch.Size([2, 32]))
assert torch.equal(q_rt, q.to(q_rt.dtype)), "selfcheck : unpack(pack(q)) != q"

# ---------- Étage 1 : 5 matrices réelles, une par famille de shape (plan §3, W2) ----------
LM = "model.language_model."
MATS = {  # nom fixture -> (module, [out, in])
    "deq_q0": (LM + "layers.0.self_attn.q_proj", [2048, 1536]),      # sliding
    "deq_q4": (LM + "layers.4.self_attn.q_proj", [4096, 1536]),      # full (head_dim 512)
    "deq_dn20": (LM + "layers.20.mlp.down_proj", [1536, 12288]),     # double-wide (KV-shared)
    "deq_plp0": (LM + "layers.0.per_layer_projection", [1536, 256]), # petite in
    "deq_plmp": (LM + "per_layer_model_projection", [8960, 1536]),   # globale
}
out_t = {}
with safe_open(CKPT, framework="pt") as f:
    for name, (mod, shp) in MATS.items():
        wp = f.get_tensor(mod + ".weight_packed")
        ws = f.get_tensor(mod + ".weight_scale")
        assert list(wp.shape) == [shp[0], shp[1] // 8], f"{mod}: packed {list(wp.shape)}"
        q_full = unpack_from_int32(wp, num_bits=4, shape=torch.Size(shp))
        deq = (q_full.reshape(shp[0], shp[1] // 32, 32).to(torch.bfloat16) * ws.unsqueeze(-1)).reshape(*shp)
        out_t[name] = deq.contiguous()

# Selfcheck piège 12 : l'expected recalculé DOIT être bit-identique à la décompression de
# RÉFÉRENCE (weights_w4dq, produite par transformers/compressed-tensors au script 55) — on ne
# fait pas confiance à notre recalcul, on le prouve contre la lib de bout en bout.
DQREF = os.path.join(ROOT, "weights_w4dq", "model.safetensors")
with safe_open(DQREF, framework="pt") as f:
    for name, (mod, _) in MATS.items():
        ref = f.get_tensor(mod + ".weight")
        assert torch.equal(out_t[name], ref), f"selfcheck : {mod} recalcul != décompression référence"

save_file(out_t, os.path.join(FX, "w4_mats.safetensors"))
json.dump(
    {
        "source": "57_w4_unpack_fixture.py étage 1 (5 familles de shape de weights_w4)",
        "pass": "dequant ZML de weight_packed/weight_scale (lus du checkpoint) == deq_*, bit-exact bf16, 5/5",
        "matrices": {k: v[1] for k, v in MATS.items()},
        "modules": {k: v[0] for k, v in MATS.items()},
    },
    open(os.path.join(FX, "w4_mats_manifest.json"), "w"), indent=2,
)
print("PASS fixtures W1/W2 écrites")
```

Note : le chemin d'import `pack_to_int32` varie selon la version de compressed-tensors
(`compressed_tensors.compressors.pack_quantized` sur main ; `compressed_tensors.compressors.quantized_compressors.pack_quantized`
sur ≤ 0.12.x) — ajuster à la version installée au Step 0.2, la fonction est bit-identique pour num_bits=4.

- [ ] **Step 3.2 : sync + run + rapatrier les manifests** (les `.safetensors` sont gitignorés —
  `fixtures/*.safetensors` dans `.gitignore` — et restent sur la VM ; seuls les manifests JSON
  entrent au repo, les fixtures se régénèrent par le script)

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
ssh $W4R 'cd /data/gemma4-zml-probe && /data/venvs/w4quant/bin/python3 scripts/57_w4_unpack_fixture.py'
rsync -av "$W4R:/data/gemma4-zml-probe/fixtures/w4_*manifest.json" /Users/regis/dev/gemma4-zml-probe/fixtures/
```

- [ ] **Step 3.3 : commit**

```bash
git add scripts/57_w4_unpack_fixture.py fixtures/w4_unpack_manifest.json fixtures/w4_mats_manifest.json
git commit -m "feat(w4): fixtures W1 (étage 0 + contre-test corruption) et W2 (5 familles de shape) — packing par la référence compressed-tensors"
```

---

### Task 4 : brique `w4.zig` + gate W1 (unpack bit-exact)

**Files:**
- Create: `zml_runner/w4.zig`
- Create: `zml_runner/gemma4_w4gate.zig`
- Modify: `zml_runner/BUILD.bazel` (ajout cible `gemma4_w4gate`)

- [ ] **Step 4.1 : écrire `zml_runner/w4.zig`**

```zig
// W4 — brique poids 4-bit w4a16 compressed-tensors (jalon J1, spec docs/superpowers/specs/
// 2026-07-18-w4-poids-4bit-12b-design.md, plan docs/superpowers/plans/2026-07-24-w4-j1-brique-e2b.md).
//
// Format (D1, vérifié sur le checkpoint Google 12B) : par Linear [out, in] :
//   weight_packed i32 [out, in/8]  — nibble j du mot w = colonne 8w+j, stocke q+8 (non signé)
//   weight_scale bf16 [out, in/32] — une scale par groupe de 32 le long de la dim d'ENTRÉE
// Dequant = (nibble - 8) * scale, en bf16 (== référence compressed-tensors _dequantize).
//
// Extraction LOGIQUE (shiftRightLogical + AND 0xF) puis -8 : le nibble est q+8 NON SIGNÉ,
// un shift arithmétique propagerait le bit 31 (nibble 7 >= 8) et fausserait les nibbles hauts.
const std = @import("std");
const zml = @import("zml");

/// Poids linéaire packé : les 2 tenseurs du store (+ tags canoniques .o/.gp et .o/.g).
pub const W4Lin = struct {
    pk: zml.Tensor, // {.o, .gp} i32, gp = in/8
    sc: zml.Tensor, // {.o, .g}  bf16, g  = in/32

    pub fn init(v: zml.io.TensorStore.View, comptime name: []const u8) W4Lin {
        return .{
            .pk = v.createTensor(name ++ ".weight_packed", .{ .o, .gp }, null),
            .sc = v.createTensor(name ++ ".weight_scale", .{ .o, .g }, null),
        };
    }

    /// dequant + retag vers les tags attendus par le site moteur (ex .{ .d, .m } pour o_proj).
    pub fn toW(self: W4Lin, tagz: anytype) zml.Tensor {
        return dequantW4(self.pk, self.sc).withTags(tagz);
    }
};

/// Unpack seul : {.o, .gp} i32 -> {.o, .d} i32, valeurs q dans [-8, 7]. Exposé pour le gate W1.
pub fn unpackW4(pk: zml.Tensor) zml.Tensor {
    var nibs: [8]zml.Tensor = undefined;
    inline for (0..8) |j| {
        nibs[j] = pk
            .shiftRightLogical(zml.Tensor.scalar(4 * j, .i32))
            .logical(.AND, zml.Tensor.scalar(0xF, .i32));
    }
    // stack en axe mineur .nib : ordre little-endian j=0..7 == colonnes 8w+0..8w+7
    const stacked = zml.Tensor.stack(&nibs, .last, .nib); // {.o, .gp, .nib=8}
    const q = stacked.sub(zml.Tensor.scalar(@as(i32, 8), .i32)); // q = nibble - 8
    return q.merge(.{ .d = .{ .gp, .nib } }); // {.o, .d = in}
}

/// Dequant complet : (unpack - fait ci-dessus) -> bf16 -> * scale par groupe de 32 -> {.o, .d} bf16.
pub fn dequantW4(pk: zml.Tensor, sc: zml.Tensor) zml.Tensor {
    const q = unpackW4(pk).convert(.bf16); // {.o, .d}
    const grouped = q.splitAxis(.d, .{ .g = .auto, .gi = 32 }); // {.o, .g, .gi=32}
    // broadcast auto binaryOp : tags identiques même ordre, sc {.o,.g,.gi=1} -> {.o,.g,.gi=32}
    const deq = grouped.mul(sc.appendAxes(.{.gi}));
    return deq.merge(.{ .d = .{ .g, .gi } }); // {.o, .d}
}
```

- [ ] **Step 4.2 : écrire `zml_runner/gemma4_w4gate.zig`** — gates CPU W1/W2/W3, un binaire.
  Squelette (pattern `gemma4_engine_e2` : registry/store/fixture, compileFn d'un mini-graphe,
  toSliceAlloc + comparaison host, verdict par erreur Zig) :

```zig
// W4 — gates W1/W2/W3 (CPU). Usage :
//   gemma4_w4gate w1 <fixtures/w4_unpack.safetensors>
//   gemma4_w4gate w2 <weights_w4/model.safetensors> <fixtures/w4_mats.safetensors>
//   gemma4_w4gate w3 <weights_w4/model.safetensors> <fixtures/w4_gemm.safetensors>
// Critères pré-enregistrés : plan §3 (W1/W2 bit-exact ; W3 max_abs<=1e-4, mean_abs<=1e-6).
const std = @import("std");
const zml = @import("zml");
const w4 = @import("w4.zig");

const log = std.log.scoped(.w4gate);

const G1 = struct { // W1 : unpack + dequant étage 0 + contre-test corruption
    pub fn forward(pk: zml.Tensor, sc: zml.Tensor, pkc: zml.Tensor) struct { zml.Tensor, zml.Tensor, zml.Tensor } {
        return .{ w4.unpackW4(pk), w4.dequantW4(pk, sc), w4.unpackW4(pkc) };
    }
};

const G2 = struct { // W2 : dequant matrices réelles (packed/scale lus du CHECKPOINT) — 1 compile/shape
    pub fn forward(pk: zml.Tensor, sc: zml.Tensor) zml.Tensor {
        return w4.dequantW4(pk, sc);
    }
};
// w2 boucle sur les 5 modules du manifest w4_mats (mêmes clés en dur : layers.0/.4 q_proj,
// layers.20 down_proj, layers.0 per_layer_projection, per_layer_model_projection) : un
// compileFn(G2.forward) PAR shape (les 5 shapes diffèrent), verdict agrégé 5/5.

const G3 = struct { // W3 : GEMM q_proj — même flux que dotPrec(fam=null) : x.dot(w.convert(f32))
    pub fn forward(x: zml.Tensor, pk: zml.Tensor, sc: zml.Tensor) zml.Tensor {
        const w = w4.dequantW4(pk, sc); // {.o, .d} bf16
        return x.convert(.f32).dot(w.convert(.f32), .d); // x {.b,.s,.d} -> {.b,.s,.o}
    }
};
// main : dispatch sur argv[1] ("w1"/"w2"/"w3"), Platform.auto (CPU), fixtures via
// TensorRegistry.fromPath + store.view().createTensor (clés du script 57/58), compileFn du
// mini-graphe, exécution, toSliceAlloc, comparaisons :
//   w1 : q == q_expected (items(i32), égalité stricte) ; deq == deq_expected (bf16 : comparer
//        les items(u16) bit à bit — bf16 lu tel quel) ; unpack(packed_corrupt) DOIT différer de
//        q_expected sur >= 1 élément (sinon error.VacuousGate).
//   w2 : pour chacune des 5 matrices : deq(checkpoint) == deq_* de la fixture, égalité u16
//        stricte ; log du module + premier index divergent + valeurs si FAIL ; verdict 5/5.
//   w3 : max_abs / mean_abs vs out_expected f32 + 8 points fixes du manifest ;
//        seuils W3 du plan ; log max_abs localisé.
// Verdict : error.GateFailed / error.VacuousGate ; sinon log "PASS w1|w2|w3" + exit 0.
```

Écrire le main complet en suivant ce contrat (le pattern exact de lecture/comparaison est dans
`gemma4_engine_e2.zig:67-140` et `gemma4_full_layer.zig:213-260` — s'y référer pendant l'écriture).
Clés fixtures : `packed`, `scales`, `q_expected`, `deq_expected`, `packed_corrupt` (w1) ;
`deq_expected` (w2) + checkpoint `model.language_model.layers.0.self_attn.q_proj.weight_packed`/`.weight_scale` ;
`x`, `out_expected` (w3). Tags à la création : `packed`/`pk` en `.{ .o, .gp }`, `scales`/`sc` en
`.{ .o, .g }`, `x` en `.{ .b, .s, .d }`.

- [ ] **Step 4.3 : ajouter la cible à `zml_runner/BUILD.bazel`** (après le bloc gemma4_bbatch/bbs)

```python
# W4 (plan docs/superpowers/plans/2026-07-24-w4-j1-brique-e2b.md) — gates W1/W2/W3 CPU de la
# brique poids 4-bit. Nom court : quota comptime @typeName pjrt structSize (piège 4).
# engine.zig dans srcs DÈS MAINTENANT : w4.zig l'importera à la Task 7 (W4LayerW.toLayerW) et
# Bazel doit exposer tout fichier @import-é au sandbox (convention BUILD.bazel:373-374).
zig_binary(
    name = "gemma4_w4gate",
    main = "gemma4_w4gate.zig",
    srcs = ["w4.zig", "engine.zig"],
    visibility = ["//visibility:public"],
    deps = ["//bazel", "//zml"],
)
```

- [ ] **Step 4.4 : deploy + build + run W1 (CPU, VM)**

```bash
ZML_REMOTE=$W4R ZML_DST=/data/rqz_workspace/zml/examples/rqz /Users/regis/dev/gemma4-zml-probe/zml_runner/deploy_to_3090.sh
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh build //examples/rqz:gemma4_w4gate 2>&1 | tail -5'
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel-bin/examples/rqz/gemma4_w4gate w1 /data/gemma4-zml-probe/fixtures/w4_unpack.safetensors 2>&1 | tail -10'
```

Attendu build : OK (sinon : premier point de friction probable = signatures exactes ; compiler,
lire l'erreur, corriger — ne jamais « supposer que ça compile », leçon du lot 27/06).
Attendu run : `PASS w1` avec le contre-test corruption divergent.
⚠ `--@zml//platforms:cuda=true` PAS nécessaire ici (gates CPU) — le repli CPU est ici le mode nominal.

- [ ] **Step 4.5 : commit + tag**

```bash
git add zml_runner/w4.zig zml_runner/gemma4_w4gate.zig zml_runner/BUILD.bazel
git commit -m "feat(w4): brique dequantW4 (unpack logique LE, -8, scale g32) + gate W1 PASS bit-exact + contre-test corruption"
git tag gate/w4-w1-pass
```

Étape optionnelle informative (ne bloque pas) : variante `bitCast(.u4)` dans le gate w1
(`pk.bitCast(.u4).convert(.i32).sub(scalar(8,.i32))` puis merge **`.{ .d = .{ .gp, .bitcast } }`**
— l'axe apposé par bitCast s'appelle `.bitcast`, pas `.nib`, cf tensor.zig:290) — si elle compile
ET passe, le noter dans W4_RESULTS.md comme piste ; sinon noter le rejet backend. Aucune décision
dessus à J1.

---

### Task 5 : gate W2 (dequant des 5 familles de shape)

**Files:** aucun nouveau (le runner et la fixture existent).

- [ ] **Step 5.1 : run W2**

```bash
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel-bin/examples/rqz/gemma4_w4gate w2 /data/gemma4-zml-probe/weights_w4/model.safetensors /data/gemma4-zml-probe/fixtures/w4_mats.safetensors 2>&1 | tee /tmp/w4_w2.log | tail -12'
```

Attendu : `PASS w2 5/5` (bit-exact bf16 sur les 5 matrices). Si ULP non nul : PUBLIER l'écart
(module, index, valeurs, pattern), comparer l'ordre des ops (mul bf16 vs upcast f32) — c'est un
vrai résultat, pas un réglage.

- [ ] **Step 5.2 : commit + tag**

```bash
git commit --allow-empty -m "gate(w4): W2 PASS — dequant 5 familles de shape bit-exact bf16 vs référence compressed-tensors"
git tag gate/w4-w2-pass
```

(Si du code a dû changer : le committer AVANT le tag, message `fix(w4): ...`.)

---

### Task 6 : gate W3 (premier GEMM W4)

**Files:**
- Create: `scripts/58_w4_gemm_oracle.py`

- [ ] **Step 6.1 : écrire `scripts/58_w4_gemm_oracle.py`**

```python
#!/usr/bin/env python3
"""W4 W3 — oracle du premier GEMM W4 (q_proj L0), flux du moteur : bf16 -> f32 GEMM.

x aléatoire seedé [1,1,1536] bf16 ; w = q_proj L0 lu dans weights_w4dq (la décompression de
RÉFÉRENCE produite au script 55 — piège 12 : jamais de recalcul maison) ;
out_expected = x.f32 @ w.f32.T  (même flux que dotPrec(fam=null) : les deux opérandes upcastés f32).
Sortie : fixtures/w4_gemm.safetensors + manifest (8 points fixes + stats).
Venv : /data/venvs/w4quant.
"""
import json
import os

import torch
from safetensors import safe_open
from safetensors.torch import save_file

ROOT = "/data/gemma4-zml-probe"
DQREF = os.path.join(ROOT, "weights_w4dq", "model.safetensors")
FX = os.path.join(ROOT, "fixtures")
os.makedirs(FX, exist_ok=True)
QPROJ = "model.language_model.layers.0.self_attn.q_proj"

torch.manual_seed(1337)
x = torch.randn(1, 1, 1536, dtype=torch.float32).to(torch.bfloat16)

with safe_open(DQREF, framework="pt") as f:
    w = f.get_tensor(QPROJ + ".weight")  # [2048, 1536] bf16, dequant par la référence

out = x.to(torch.float32) @ w.to(torch.float32).t()  # [1, 1, 2048]
assert not torch.isnan(out).any()

save_file({"x": x.contiguous(), "out_expected": out.contiguous()}, os.path.join(FX, "w4_gemm.safetensors"))
json.dump(
    {
        "source": "58_w4_gemm_oracle.py (q_proj L0, flux bf16->f32 GEMM = dotPrec fam=null)",
        "pass": "max_abs <= 1e-4, mean_abs <= 1e-6, 8 points fixes",
        "fixed_points_out_0_0_0_8": out[0, 0, :8].tolist(),
        "stats": {"max": float(out.max()), "min": float(out.min()), "mean": float(out.mean())},
    },
    open(os.path.join(FX, "w4_gemm_manifest.json"), "w"), indent=2,
)
print("PASS fixture W3 écrite")
```

- [ ] **Step 6.2 : câbler le gate w3 dans `gemma4_w4gate.zig`** (déjà squeletté Task 4) —
  x créé `.{ .b, .s, .d }` bf16 depuis la fixture, comparaison f32 max_abs/mean_abs + 8 points
  fixes copiés du manifest dans le code (pattern L_BLOCKS de `gemma4_full_layer.zig`).

- [ ] **Step 6.3 : sync + run W3**

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
ssh $W4R 'cd /data/gemma4-zml-probe && /data/venvs/w4quant/bin/python3 scripts/58_w4_gemm_oracle.py'
rsync -av $W4R:/data/gemma4-zml-probe/fixtures/w4_gemm_manifest.json /Users/regis/dev/gemma4-zml-probe/fixtures/
ZML_REMOTE=$W4R ZML_DST=/data/rqz_workspace/zml/examples/rqz /Users/regis/dev/gemma4-zml-probe/zml_runner/deploy_to_3090.sh
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh build //examples/rqz:gemma4_w4gate && ./bazel-bin/examples/rqz/gemma4_w4gate w3 /data/gemma4-zml-probe/weights_w4/model.safetensors /data/gemma4-zml-probe/fixtures/w4_gemm.safetensors 2>&1 | tail -10'
```

Attendu : `PASS w3`, max_abs ≲ 3e-5 (précédent o_proj/q_proj : 2.29e-5 sur réduction 2048).

- [ ] **Step 6.4 : commit + tag**

```bash
git add scripts/58_w4_gemm_oracle.py fixtures/w4_gemm_manifest.json zml_runner/gemma4_w4gate.zig
git commit -m "feat(w4): W3 PASS — premier GEMM W4 (q_proj L0) == oracle torch fp32, flux dotPrec"
git tag gate/w4-w3-pass
```

---

### Task 7 : runner `gemma4_w4auto` (décode complet, GPU)

**Files:**
- Create: `zml_runner/gemma4_w4auto.zig` (copie de `gemma4_gen_auto.zig` + diff ciblé ci-dessous)
- Modify: `zml_runner/w4.zig` (ajout W4LayerW/W4Model)
- Modify: `zml_runner/BUILD.bazel`

- [ ] **Step 7.1 : ajouter à `w4.zig` les structures modèle** (miroir de `LayerW`/`EngineModel.initWith`,
  clés compressed-tensors pour les 9 linears, clés `.weight` bf16 pour le reste) :

```zig
const engine = @import("engine.zig");

/// K/V/k_norm d'une couche PRODUCER (0-14). Les 20 readers YOCO (15-34) n'ont PAS ces modules
/// dans le checkpoint QAT (num_kv_shared_layers=20) — créer les clés planterait (crash .? sur
/// clé absente, zml/io.zig:168). Struct séparé -> slice de 15, chargée comme le reste.
pub const W4KV = struct {
    k: W4Lin,
    k_norm: zml.Tensor,
    v: W4Lin,

    pub fn init(sa: zml.io.TensorStore.View) W4KV {
        return .{
            .k = .init(sa, "k_proj"),
            .k_norm = sa.createTensor("k_norm.weight", .{.hd}, null),
            .v = .init(sa, "v_proj"),
        };
    }
};

/// Poids d'une couche E2B au format W4 : 7 linears packés + norms/scalar bf16 — SANS k/v
/// (portés par W4KV pour les seuls producers).
pub const W4LayerW = struct {
    input_layernorm: zml.Tensor,
    q: W4Lin, q_norm: zml.Tensor,
    o: W4Lin,
    post_attention_layernorm: zml.Tensor,
    pre_feedforward_layernorm: zml.Tensor,
    gate: W4Lin, up: W4Lin, down: W4Lin,
    post_feedforward_layernorm: zml.Tensor,
    plig: W4Lin, plp: W4Lin,
    post_per_layer_input_norm: zml.Tensor,
    layer_scalar: zml.Tensor,

    pub fn init(v: zml.io.TensorStore.View) W4LayerW {
        const sa = v.withPrefix("self_attn");
        const mlp = v.withPrefix("mlp");
        return .{
            .input_layernorm = v.createTensor("input_layernorm.weight", .{.d}, null),
            .q = .init(sa, "q_proj"), .q_norm = sa.createTensor("q_norm.weight", .{.hd}, null),
            .o = .init(sa, "o_proj"),
            .post_attention_layernorm = v.createTensor("post_attention_layernorm.weight", .{.d}, null),
            .pre_feedforward_layernorm = v.createTensor("pre_feedforward_layernorm.weight", .{.d}, null),
            .gate = .init(mlp, "gate_proj"), .up = .init(mlp, "up_proj"), .down = .init(mlp, "down_proj"),
            .post_feedforward_layernorm = v.createTensor("post_feedforward_layernorm.weight", .{.d}, null),
            .plig = .init(v, "per_layer_input_gate"), .plp = .init(v, "per_layer_projection"),
            .post_per_layer_input_norm = v.createTensor("post_per_layer_input_norm.weight", .{.d}, null),
            .layer_scalar = v.createTensor("layer_scalar", .{.one}, null),
        };
    }

    /// Dequant en graphe -> LayerW moteur, tags par site == LayerW.init (engine.zig:179-201).
    /// kv = null pour les readers : leurs champs k_proj/k_norm/v_proj reçoivent un PLACEHOLDER
    /// INERTE (tenseur existant, AUCUNE op émise) — jamais consommé au traçage, prouvé par
    /// engine.zig:415-423 (runLayerGen : la branche K/V est dans le `else` de `if (reader)`,
    /// les readers lisent les slots writers). Un placeholder consommé par erreur casserait
    /// au compile (shape/tags faux) — pas de faux silencieux possible.
    pub fn toLayerW(self: W4LayerW, kv: ?W4KV) engine.LayerW {
        return .{
            .input_layernorm = self.input_layernorm,
            .q_proj = self.q.toW(.{ .o, .d }), .q_norm = self.q_norm,
            .k_proj = if (kv) |x| x.k.toW(.{ .o, .d }) else self.input_layernorm,
            .k_norm = if (kv) |x| x.k_norm else self.input_layernorm,
            .v_proj = if (kv) |x| x.v.toW(.{ .o, .d }) else self.input_layernorm,
            .o_proj = self.o.toW(.{ .d, .m }),
            .post_attention_layernorm = self.post_attention_layernorm,
            .pre_feedforward_layernorm = self.pre_feedforward_layernorm,
            .gate_proj = self.gate.toW(.{ .f, .d }), .up_proj = self.up.toW(.{ .f, .d }),
            .down_proj = self.down.toW(.{ .d, .f }),
            .post_feedforward_layernorm = self.post_feedforward_layernorm,
            .per_layer_input_gate = self.plig.toW(.{ .p, .d }),
            .per_layer_projection = self.plp.toW(.{ .d, .p }),
            .post_per_layer_input_norm = self.post_per_layer_input_norm,
            .layer_scalar = self.layer_scalar,
        };
    }
};

/// Modèle E2B-W4 complet (miroir EngineModel.initWith, engine.zig:531-543).
pub const W4Model = struct {
    embed_tokens: zml.Tensor,   // bf16 (ignore re:.*embed.*)
    plmp: W4Lin,                // per_layer_model_projection : quantifié
    per_layer_projection_norm: zml.Tensor,
    final_norm: zml.Tensor,
    layers: []W4LayerW,         // 35 (champs communs)
    kv: []W4KV,                 // 15 (producers 0-14 seulement)

    pub fn init(allocator: std.mem.Allocator, base: zml.io.TensorStore.View) !W4Model {
        const layers = try allocator.alloc(W4LayerW, engine.NUM_LAYERS);
        const kv = try allocator.alloc(W4KV, engine.FIRST_KV_SHARED);
        const lb = base.withPrefix("layers");
        for (layers, 0..) |*l, i| l.* = W4LayerW.init(lb.withLayer(i));
        for (kv, 0..) |*x, i| x.* = W4KV.init(lb.withLayer(i).withPrefix("self_attn"));
        return .{
            .embed_tokens = base.createTensor("embed_tokens.weight", .{ .voc, .d }, null),
            .plmp = .init(base, "per_layer_model_projection"),
            .per_layer_projection_norm = base.createTensor("per_layer_projection_norm.weight", .{.p}, null),
            .final_norm = base.createTensor("norm.weight", .{.d}, null),
            .layers = layers,
            .kv = kv,
        };
    }

    pub fn load(self: *const W4Model, allocator: std.mem.Allocator, io: std.Io, platform: *const zml.Platform, store: *const zml.io.TensorStore, shardings: []const zml.sharding.Sharding) !zml.Bufferized(W4Model) {
        return zml.io.load(W4Model, self, allocator, io, platform, store, .{ .shardings = shardings, .parallelism = 1, .dma_chunks = 1, .dma_chunk_size = 16 * 1024 * 1024 });
    }
};
```

- [ ] **Step 7.2 : `cp zml_runner/gemma4_gen_auto.zig zml_runner/gemma4_w4auto.zig`** puis appliquer
  UNIQUEMENT ce diff (tout le reste — RoPE host, chat template, tokenizer, boucle, garde VRAM 20 GiB,
  garde CudaRequired, flags — reste à l'identique) :

  1. En-tête : commentaire W4 (réf plan + spec), `const w4 = @import("w4.zig");`.
  2. `StepTok` → `W4Step` :

```zig
const W4Step = struct {
    pub fn forward(m4: w4.W4Model, tabs: Tabs, tok: zml.Tensor, p: PackedLong, cache: engine.Cache, ctrl: engine.Ctrl) struct { zml.Tensor, zml.Tensor, zml.Tensor, zml.Tensor, zml.Tensor, zml.Tensor } {
        // Assemblage par VALEUR d'un Model moteur dont les poids linéaires sont des sous-graphes
        // dequantW4 — forwardStep INCHANGÉ (engine.zig:661). Les Tensors sont des valeurs de
        // traçage : le tableau vit le temps du trace.
        var layers: [engine.NUM_LAYERS]engine.LayerW = undefined;
        for (m4.layers, 0..) |lw, i|
            layers[i] = lw.toLayerW(if (i < engine.FIRST_KV_SHARED) m4.kv[i] else null);
        const m: Model = .{
            .embed_tokens = m4.embed_tokens,
            .per_layer_model_projection = m4.plmp.toW(.{ .lf, .d }),
            .per_layer_projection_norm = m4.per_layer_projection_norm,
            .final_norm = m4.final_norm,
            .layers = &layers,
            .brick = .{},
            .prec = .{},
        };
        const e = m.embed_tokens.gather(.{ .voc = tok }, .{});
        const el = tabs.eptl.gather(.{ .voc = tok }, .{});
        const logits, const slk, const slv, const flk, const flv = m.forwardStep(e, el, p, cache, ctrl);
        const t5 = logits.topK(.{ .voc = .voc }, 5, .{});
        return .{ t5.values, t5.indices, slk, slv, flk, flv };
    }
};
```

  3. `const model: Model = try .init(...)` → `const model: w4.W4Model = try .init(arena.allocator(), base)`
     (même `base` withPrefix — les clés W0 gardent le préfixe `model.language_model.`).
  4. `compileFn(..., StepTok.forward, .{ model, tabs, ... })` → `W4Step.forward` (mêmes args).
  5. `model.load(...)` → inchangé dans la forme (`w4.W4Model.load`).
  6. `--selftest-gather`/`--selftest-inputs` : conservés tels quels (EMB_KEY absolue = `embed_tokens.weight`
     bf16, toujours présent dans weights_w4).
  7. `usage`/logs : renommer `gemma4_gen_auto` → `gemma4_w4auto` ; noter dans l'usage que
     `<model.safetensors>` = checkpoint **w4a16-ct**.
  8. Marge top1−top2 : le decode loggue déjà top-5 (t5.values) en mode oracle — vérifier que le log
     par step imprime `top1-top2` ; sinon l'ajouter (1 ligne au point de lecture des t5.values).

- [ ] **Step 7.3 : BUILD.bazel**

```python
# W4 — runner décode E2B-W4 complet (GPU). Clone ciblé de gemma4_gen_auto (duplication assumée,
# précédent bbs) : seuls Model/StepTok/init changent, engine.zig INTACT.
zig_binary(
    name = "gemma4_w4auto",
    main = "gemma4_w4auto.zig",
    srcs = ["engine.zig", "mem_probe.zig", "w4.zig"],
    visibility = ["//visibility:public"],
    deps = ["//bazel", "//zml"],
)
```

- [ ] **Step 7.4 : deploy + build GPU** (c'est ICI que le risque « Model assemblé à la main » se lève)

```bash
ZML_REMOTE=$W4R ZML_DST=/data/rqz_workspace/zml/examples/rqz /Users/regis/dev/gemma4-zml-probe/zml_runner/deploy_to_3090.sh
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh build --@zml//platforms:cuda=true //examples/rqz:gemma4_w4auto 2>&1 | tail -5'
```

Attendu : build OK. Frictions probables et parades :
- erreur comptime sur la construction de `Model` par valeur → vérifier champs/ordre vs `EngineModel` (engine.zig:516-525) ;
- quota comptime → le patch pjrt est en place (vérifié) ; si ça déborde quand même, raccourcir les noms de types ;
- `zml.io.load` sur `W4Lin` imbriqué → même mécanique de réflexion que `LayerW` (structs plats de Tensors), pas de raison d'échec — sinon aplatir les champs dans `W4LayerW`.

- [ ] **Step 7.5 : commit**

```bash
git add zml_runner/w4.zig zml_runner/gemma4_w4auto.zig zml_runner/BUILD.bazel
git commit -m "feat(w4): runner gemma4_w4auto — W4Model/W4Step (dequant in-graph, forwardStep intact), build CUDA OK"
```

---

### Task 8 : gate W4g — décode E2B-W4 48/48 == HF-même-checkpoint + contre-test + mesures

**Files:**
- Create: `scripts/59_w4_corrupt_ckpt.py`

Avant CHAQUE run GPU de cette task (piège 16 — un refus de garde n'est pas un FAIL de fidélité) :

```bash
ssh $W4R 'while [ "$(nvidia-smi --query-compute-apps=pid --format=csv,noheader | wc -l)" -ne 0 ]; do sleep 2; done; echo GPU_LIBRE'
```

- [ ] **Step 8.1 : run gate (GPU, oracle produit en Task 2)**

```bash
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh run --@zml//platforms:cuda=true //examples/rqz:gemma4_w4auto -- /data/gemma4-zml-probe/weights_w4/model.safetensors /data/gemma4-zml-probe/gemma4-e2b-it-meta/tokenizer.json --prompt "What is the capital of France? Answer in one word." --oracle /data/gemma4-zml-probe/w4_gen48.safetensors 2>&1 | tee /tmp/w4g.log | tail -25'
rsync -av $W4R:/tmp/w4g.log /Users/regis/dev/gemma4-zml-probe/logs/w4g.log
```

Critère et requalification : pré-enregistrés §3 (48/48 = PASS ; N<48 = FAIL par défaut, la
requalification différentielle exige marge ≤ 1e-3 + re-run documenté + décision explicite de Régis).
En cas de flip : lire la marge top1−top2 au step fautif AVANT tout verdict (piège 17) ; un écart
massif = bug, retour aux gates W1-W3. Le log reste local (`logs/` est gitignoré) — les chiffres
vont dans W4_RESULTS.md (convention BATCHING_RESULTS : « la doc porte les résultats »).

- [ ] **Step 8.2 : contre-test de non-vacuité (LOGITS, piège 13) — câblé, pas déclaratif**

Écrire `scripts/59_w4_corrupt_ckpt.py` :

```python
#!/usr/bin/env python3
"""W4 W4g — contre-test de non-vacuité : copie du checkpoint avec UN weight_scale corrompu (x4).

Cible : model.language_model.layers.17.self_attn.q_proj.weight_scale (couche médiane).
Le décode gemma4_w4auto --oracle sur cette copie DOIT sortir en error.GenMismatch (divergence
argmax) : un 48/48 sur checkpoint corrompu prouverait que le chemin scale n'est pas consommé.
Sortie : /data/gemma4-zml-probe/weights_w4_corrupt.safetensors
"""
import torch
from safetensors import safe_open
from safetensors.torch import save_file

SRC = "/data/gemma4-zml-probe/weights_w4/model.safetensors"
DST = "/data/gemma4-zml-probe/weights_w4_corrupt.safetensors"
KEY = "model.language_model.layers.17.self_attn.q_proj.weight_scale"

out = {}
with safe_open(SRC, framework="pt") as f:
    for k in f.keys():
        t = f.get_tensor(k)
        if k == KEY:
            t = t * 4.0
        out[k] = t.contiguous()
save_file(out, DST)
print(f"PASS : {DST} écrit ({KEY} x4)")
```

Puis (GPU libre — précheck ci-dessus) :

```bash
rsync -av /Users/regis/dev/gemma4-zml-probe/scripts/ $W4R:/data/gemma4-zml-probe/scripts/
ssh $W4R 'cd /data/gemma4-zml-probe && /data/venvs/w4quant/bin/python3 scripts/59_w4_corrupt_ckpt.py'
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh run --@zml//platforms:cuda=true //examples/rqz:gemma4_w4auto -- /data/gemma4-zml-probe/weights_w4_corrupt.safetensors /data/gemma4-zml-probe/gemma4-e2b-it-meta/tokenizer.json --prompt "What is the capital of France? Answer in one word." --oracle /data/gemma4-zml-probe/w4_gen48.safetensors 2>&1 | tee /tmp/w4g_corrupt.log | tail -8'
rsync -av $W4R:/tmp/w4g_corrupt.log /Users/regis/dev/gemma4-zml-probe/logs/
```

Attendu : le run ÉCHOUE **par `error.GenMismatch`** (divergence détectée par le banc), PAS par un
crash (leçon « le contrôle qui ne peut pas réussir » : un contre-test doit échouer de la façon
prévue). S'il PASSE 48/48 : gate W4g VIDE → chercher pourquoi le scale n'influe pas.
Ménage : `ssh $W4R 'rm /data/gemma4-zml-probe/weights_w4_corrupt.safetensors'` après verdict.

- [ ] **Step 8.3 : démo texte libre (spec « décode complet », plaisir inclus)** (GPU libre — précheck)

```bash
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh run --@zml//platforms:cuda=true //examples/rqz:gemma4_w4auto -- /data/gemma4-zml-probe/weights_w4/model.safetensors /data/gemma4-zml-probe/gemma4-e2b-it-meta/tokenizer.json --prompt "Explique en une phrase ce qu'est la quantization 4-bit." --max-tokens 80 2>&1 | tee /tmp/w4_demo.log | tail -10'
```

Observation (pas critère) : réponse FR cohérente + tok/s loggé (référence bf16 : 113 tok/s ; le
dequant par step a un coût — chiffre à consigner, R4 : correctness first).

- [ ] **Step 8.4 : mesure VRAM réelle** (piège 14 : `--no-prealloc` sinon on lit la réserve BFC ;
  GPU libre — précheck). Reproduire le protocole G3 : build temporaire `preallocate=false` OU
  échantillonnage `nvidia-smi --query-compute-apps=used_memory` (boucle 1 s, `tee /tmp/w4_vram.log`)
  pendant le run du Step 8.1 relancé. Consigner : pic MiB, delta vs bf16 (16 658 MiB), part poids
  attendue ≈ eptl 4,7 G + embed 0,8 G + packé ~1,1 G. (Sur E2B le gain est borné par les embeddings
  bf16 — c'est ATTENDU, la brique vise le 12B où les linears dominent. Le noter tel quel dans
  W4_RESULTS.md.)

- [ ] **Step 8.5 : commit + tag** (les logs restent locaux, gitignorés — les chiffres vont en Task 10
  dans W4_RESULTS.md)

```bash
git add scripts/59_w4_corrupt_ckpt.py
git commit -m "gate(w4): W4g PASS — décode E2B-W4 GPU 48/48 == HF-même-checkpoint ; contre-test scale-corrompu GenMismatch OK ; débit X tok/s, pic VRAM Y MiB (--no-prealloc)"
git tag gate/w4-w4g-pass
```

---

### Task 9 : WN — moteur intact + non-régression

**Files:** aucun.

- [ ] **Step 9.1 : preuve engine intact**

```bash
git diff --stat main -- zml_runner/engine.zig   # attendu : AUCUNE sortie
```

- [ ] **Step 9.2 : non-régression du témoin `gemma4_gen_auto`** (les srcs partagés ont bougé —
  BUILD/w4.zig — le témoin doit re-passer à l'identique)

```bash
# GPU libre d'abord (piège 16) :
ssh $W4R 'while [ "$(nvidia-smi --query-compute-apps=pid --format=csv,noheader | wc -l)" -ne 0 ]; do sleep 2; done; echo GPU_LIBRE'
ssh $W4R 'cd /data/rqz_workspace/zml && ./bazel.sh run --@zml//platforms:cuda=true //examples/rqz:gemma4_gen_auto -- /data/gemma4-zml-probe/weights/model.safetensors /data/gemma4-zml-probe/gemma4-e2b-it-meta/tokenizer.json --prompt "What is the capital of France? Answer in one word." --oracle /data/gemma4-zml-probe/gen_custom.safetensors 2>&1 | tee /tmp/w4_wn.log | tail -8'
rsync -av $W4R:/tmp/w4_wn.log /Users/regis/dev/gemma4-zml-probe/logs/
# smoke SANS étouffer le code de sortie (le critère WN doit pouvoir échouer — smoke.sh exit = nb de fails) :
ssh $W4R 'cd /data/gemma4-zml-probe && SMOKE_TARGETS="gemma4_engine_e1 gemma4_w4gate gemma4_w4auto gemma4_gen_auto" bash scripts/smoke.sh'
echo "smoke exit=$?"
```

(`smoke.sh` fait lui-même `cd $ZML_WS` — il se lance depuis n'importe où sur la VM ; les scripts
ont été rsyncés en Task 0/3.) Attendu : oracle re-PASS + `smoke exit=0` (4 builds OK). Tout autre
exit = WN FAIL.

- [ ] **Step 9.3 : commit + tag**

```bash
git commit --allow-empty -m "gate(w4): WN PASS — engine.zig 0 octet modifié, témoin gen_auto re-PASS, smoke builds OK"
git tag gate/w4-wn-pass
```

---

### Task 10 : documentation + clôture de branche

**Files:**
- Create: `docs/W4_RESULTS.md`
- Modify: `docs/DOCUMENTATION.md`, `PLANNING.md`

- [ ] **Step 10.1 : `docs/W4_RESULTS.md`** — structure : contexte (1 §), format D1 (le tableau
  layout vérifié sur le 12B), gates W0→WN avec verdicts/chiffres/logs, observations (débit, VRAM,
  variante bitCast si testée), pièges neufs rencontrés, décision J2 EN ATTENTE (règle d'arrêt :
  GO re-décidable par Régis — le plan J2 n'existe pas encore, c'est voulu).
- [ ] **Step 10.2 : `docs/DOCUMENTATION.md`** — §2 nouvelle capacité « poids 4-bit w4a16 » ;
  §5.1 scripts 54-59 ; §5.2 runners w4gate/w4auto + w4.zig ; §7 chiffres W4g ; §8 pièges neufs
  (numérotation continue ≥ 20) ; §9 backlog (J2 12B au tiroir) ; §10 ligne historique W4-J1.
  Rattrapage des retards du tableau §5.1/5.2 (52/53/60/61, bbatch/gen_auto absents) : OUI, au passage
  (portée : lignes de tableau uniquement).
- [ ] **Step 10.3 : `PLANNING.md`** du repo — graine W4 → « J1 livré », lien W4_RESULTS.md.
- [ ] **Step 10.4 : commit final + PR**

```bash
git add docs/W4_RESULTS.md docs/DOCUMENTATION.md PLANNING.md
git commit -m "docs(w4): W4_RESULTS + DOCUMENTATION (capacité 4-bit, scripts 54-59, runners, pièges) + PLANNING — jalon J1 clos"
git push -u origin w4-brique
gh pr create --title "W4-J1 : brique poids 4-bit w4a16 dans ZML, prouvée sur E2B (gates W0-WN PASS)" --body "..."
```

(PR vers `main`, merge après revue — décision Régis. Branche conservée après merge si Régis le veut,
précédent generation-longue.)

---

## Notes de fin

- **Taille estimée** : 1 à 2 sessions (runs GPU courts ~40 s-2 min ; le plus long = W0a quantization
  + downloads ~10 min). Tokens : chantier « meute » assumé (ultracode session du 24 juil).
- **Règle d'arrêt (spec §7)** : J1 clos = valeur acquise (brique réutilisable, terrain TurboQuant-poids).
  GO J2 (12B Unified, gates U0-U10) = décision Régis séparée, avec les infos J2 déjà en poche :
  manifest 12B vérifié (group 32, 328 linears, **pas de v_proj sur les 8 couches full** —
  `attention_k_eq_v=true`, à intégrer à la carte des diffs U0).
- **Ce qui peut invalider ce plan** : bump du workspace ZML VM (pièges 4/7 à re-vérifier), changement
  des checkpoints HF (repo 12B a bougé le 20 juil — sha figé au download), venv w4quant impossible
  à construire (replis W0 en cascade).
